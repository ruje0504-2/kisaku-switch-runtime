# 移植交接：剩余工作与验证边界

更新：2026-09-23。工作源码在 Codex/kisaku-switch；保龄球接通后才同步到 deepseek/kisaku-switch。同步清单与来源 commit 见目标 local/source-sync/，不覆盖目标 Git 历史。仍是开发预览，不能称完整移植完成。

## 本轮已实现

- 31/11 修正 bank/cell/target 参数和双轨启动，解决原先把启动当停止的眨眼接口错误。
- 31/522 保留前周面板、修复工作文字行和问号图集坐标，增加20ms移动及问号交替渐变。动作4按原版弹出item/group，修复把group0当成无操作、导致问号不停止及结果图片文字不出现。
- 31/524日期使用独立120×128图层，在(16,16)合成，跟随显式显示/隐藏/暂停/恢复；选择界面重绘不会将其覆盖，隐藏时只恢复覆盖区域。
- 参数条演出隔离确认、取消、指针与消息菜单动作，点击不再提前结束。结束音按播放时钟到末尾自动收尾，不能等待仍供混音使用的PCM缓冲释放。
- 31/612/0、1、2 接通保龄球构建、前后两半场、结果返回和释放；包括原版教程、九种对手、鼠标/触摸/手柄投球、碰撞、摆瓶/扫瓶、球瓶姿态、比分、角色动作、AX连击/补球演出、独立音效与300分演出。
- 保龄球操作：鼠标/触摸按住向上拖动后松开；Switch左摇杆移动、按住A向上移动后松开；主机方向键配合空格。教程左右翻页、确认开始，结果确认返回。小游戏中禁止存档。

## 用户确认的范围调整

- 2026-09-22：文字阴影不要求实现，不作为移植完成的阻塞项；其余回看功能继续补齐。

## 确认仍未完整实现

- **附录剩余验证**：CG、音乐和视频入口已经接通；音乐按原版10首目录与3260..3269解锁字节，视频按原版78槽与3600..3677解锁字节返回`video.mes`继续播放。完整视频槽逐项回放、Switch实机和全路线仍未验证。秃作完整剧情及全部后期脚本仍未验证。回想入口可进入`scene.mes`场景选择模态。

1. **完整回看记录仍有缺口**：实时前端已读取原生命令记录，隔离VM重放赋值/运算/文字/换行，支持10/0字号、17/5多语音收集与顺序播放；70条记录滚动、真实开场记录和取消隔离已测。新存档已通过0xfffd v2保存原生命令、多语音命令、全部槽与录制位置，旧v1扁平历史兼容读取；70条记录及真实检查点往返已测。跨记录文字状态现在按时间顺序在隔离VM中继承；23/5 已用有界 VM 地址映射接通原生命令向量查询，含长度、嵌入 NUL、不可变快照与生命周期管理；其余未知重放原生调用同样不伪造成功。23/7文本查询、23/8外层槽数量查询和29更新标志事件泵已接通，23/8 不是 23/5 payload 长度，详见 `reports/backlog-native.md`。
2. **31/523逐帧演出已接通**：六个画面分别呈现，末步等待结束才放行VM；508540的独立20ms等待和16倍加速已静态核对。实现保留60Hz帧驱动的时间余数，普通模式按2,1,1,1,1,2帧跨越六个20ms截止点（合计8帧，避免固定两帧的200ms拉伸），仍未做PC毫秒时钟和Switch实机画面对照。
3. **角色簿细节**：存档group5的八角色解锁条目、共享标记、完成章、图片查看、进退与淡化，以及 `statfr.akb` 普通/进度/特殊/完成状态条分支均已接通（4ac3b0/4b2110/4b2820，Momo/Nadeshiko/Aoi 的专用完成行已校正）；剧情角色状态/语音组合仍未全量对照原版，不能把存档详情子集等同整项完成。
4. **受限调用和脚本体**：bootstrap保留未知原生调用明确报错；choice body副作用仍拒绝。未做全部脚本可达路径审计，不能把已实现接口列表等同于所有分支支持。
5. **精确平台绘制差异**：保龄球球路提示用便携方笔Bresenham替代GDI画线，未保证笔端/边界逐像素相同；设置暂停剧情、手柄指针等属于移植适配，尚非全部Win32时钟/交互的逐条复刻。

## 已实现但未验证完毕

- Switch真实设备的启动、保龄球操作、帧率、音频输出、长时间内存及回到剧情：**未验证**。
- 女性角色眨眼：用户确认当前表现正常，不再列为未完成项。每周面板在真实剧情中的全部组合与问号动画仍需逐场景视觉回归。
- 全路线、后期脚本、全部小游戏入口及后期存读档：**未验证**。
- 普通/另一模式回想的完整解锁目录和每一场回放：已接通，不代表逐槽全覆盖。
- 保龄球原版 bowl1st.mes/bowl2nd.mes 直接入口测试通过，真实素材两半场也通过；这是从初始化运行时进入原始脚本的测试，不能替代从正常剧情走到小游戏、所有随机种子或全部难度验证。
- 未运行PC原版程序。实现依据本地反编译与汇编静态核对；无PC实时画面对照结果。

## 后续建议顺序

按主人最新顺序：①完整回看 → ②31/523精确20ms时序 → ③角色簿剩余细节 → ④受限调用与脚本体 → 鬼作PC反汇编参数校验（逐条核对沿用河源崎2的解释）→ ⑤精确平台绘制差异。继续用原始MES与真实资源做分支覆盖和后期存档检查。未知接口保留参数并报错，不用空操作伪造成功。鬼作/保持只读，不提交EXE、素材、存档或交付包，不自动推送。

## 验证入口

`./build-host.sh`、`./test-host.sh 鬼作`、`./build-switch.sh`、`python3 tools/package_sd.py 鬼作`。保龄球专项包含score/player/throw/motion/contact/world/cpu-throw/session/projection/rack/draw/turn/game；真实MES与模态测试位于tests/kisaku_bootstrap_test.c。最终结果和NRO哈希见reports/porting.md最新节。

- **CG鉴赏已接通**：31/310普通/秃作分类、锁定、变体、大图脚本隔离播放与返回已实现并通过主机专项；完整逐槽实机回放仍未验证。附录音乐31/340、视频31/70已接通；完整视频槽逐项回放和实机仍未验证。

## 2026-09-22 显示后处理后续

- 普通消息/选项及整页信件、旧小说兼容路径已接960×720字形重绘，包含逐行显示、隐藏恢复、清页与信件存档重建；整屏清页/隐藏/恢复/退出淡化及 CMesFadeSprite 三种模式的高清字形已接通；普通旧档当前句的高清重建仍待补齐。HOS系统面板字体分工保持原状。
- CScMode/CHageScMode图集、原生解锁、特殊子列表及scene.mes/hage_scmode.mes返回调度已接通，移除旧khistory桥接；缩略图轮播、470ba0条件重排、变体计数及进退/切页淡化已接通（2026-09-23）；普通按131个15ms tick循环/80 tick淡化，秃作按92/40 tick处理，实机时钟与视觉仍未逐项对照。主机已核对普通分支和秃作第30项脚本跳转，不能据此称全部回想逐槽播放通过。
- 周末“秘伝書”已用真实 sat01_2.mes 复测：书目列表高清字形、锁定/解锁目录及第一本书脚本/参数演出通过；修复 byte1500 允许取消时未返回上层菜单的遗漏。此前照片中的其他视觉异常仍未在当前代码复现，不能称已全部修复。
- 新呈现路径Switch实机性能/效果未验证。

## 高清显示回归修复后的验证边界

- 共用GLES呈现的通道处理和SDL状态恢复已修正，真实主机GLES完成高清文字/菜单像素验证；Switch实机的鉴赏、历史页和高清文字仍未验证。
- 2026-09-23：原暂存轮播/动画草稿已按反编译审核、修正并接入主源码；local/pending-scene-animation.patch及local/pending-scene-mode-thumbnails.inc仅是历史草稿，不再代表未合入差异。真实图集的189个普通页面/进度组合及4页秃作、脚本回放返回、GLES菜单回归已测；全场景解锁组合及实机仍未全覆盖。

- 本轮视频鉴赏成功条件写反已修正；六类菜单和高清文字已做实际SDL/GLES像素回归，仍不能替代Switch实机操作验证。

- 2026-09-23：高清呈现准备已分配Switch core 2，合成去重、GPU通道交换、底图/CG纹理复用和文字变化行上传已实现；主机像素一致与线程竞态专项通过。Switch实机负载、帧率和长时运行仍待验证，不能把第三核分工视为性能目标已在实机达成。

## 当前优先实现项（2026-09-23）

1. 周末“秘伝書”可取消子菜单的返回遗漏已修复；继续覆盖其他周次/书目及照片中的视觉差异，核对剧情角色参数界面的剩余分支。
2. 回看23/5命令向量已接通；继续审计受限原生调用和choice body副作用，不把未知调用当成功。
3. 信件及旧小说兼容路径整页、逐行、清页/退出淡化和CMesFadeSprite高清已接通；普通旧档当前句的高清字形重建仍待补齐。

全路线/后期存档/全部视频槽、原版毫秒级时序及Switch实机覆盖仍为验证缺口，不能与上述实现缺口混为一谈。

### 2026-09-23 实机照片反馈的接口修复

日记31/527/0现在保持存档模式，并支持脚本隐藏消息窗口后的合法检查点保存/读回；剧情内角色档案31/910已接入当前FLAG的全宽模态，与存档角色预览分开。周面板重复present不再把自身写进底图，移动/隐藏后的背景逐像素回归已加入。详情见porting.md；角色全页进退的原版双表面分阶段精确时序、全部后期状态组合和Switch实机照片对照仍未验证。

## 2026-09-24 视频目录实资源审计

- 新发现并修复：目录预览 MOV 使用版本2，旧解析器只接受版本1。版本2命令步长为276字节（12字节操作数加264字节字符串），播放音效指令使用命令内文件名；现在按实际格式解析，保持循环、分段和入口切换行为，并在音效资源变化时替换缓存。
- `tests/video_test.c` 对EXE目录表的78项逐项读取MOV及引用VSD，执行控制命令、设置实际帧区间、解码并再次进入循环段。该检查加入主机测试。
- 这不是78段完整剧情从video.mes选择到返回的覆盖，也不是自然解锁或全路线通关。全路线、后期脚本、Switch实机仍待验证。
- 未增加游戏未使用的VM操作码，也未取消未知原生调用/回放调用和选择体副作用的错误边界。负等待涉及原版绝对时钟回绕，未根据猜测映射为零等待。

## 2026-09-24 正篇推进与存档事件修复

- 修正 `31/527` 存档/读档请求的重复轮询：同一原生事件边沿在菜单关闭前不会重复打开模态；保存菜单按保存/读取模式释放对应边沿，后续进入标题读档仍可重新触发。
- 修正真实正篇探针对 `31/910` 剧情角色状态模态的关闭处理，保留脚本返回值，不再用探针强写全局变量改变分支。
- 四种自然选择策略已从标题进入开场、`lev00_1.mes`、角色状态入口并能回到标题；主机全量回归通过。该结果证明对应已接通路径没有崩溃或未实现调用，但仍不等于所有结局、全部后期路线和所有自然解锁组合已经逐一通关。

- 2026-09-24：路线探针改为按菜单记录访问次数，并把带 `byte1500` 的可取消菜单按原生 B 返回处理；此前连续确认单项菜单会反复启动同一角色事件，造成假性“卡死”。固定重复选择同一非取消选项仍会进入原脚本的重复事件循环并增长操作数栈，不能用清栈伪造路线推进。`check_routes.py` 现在优先使用运行时目录内的 `鬼作` 资源；交替选择路线已实际进入 `end01.mes` 并返回标题。
