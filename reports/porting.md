# 《鬼作》AI6WIN 移植记录

日期：2026-09-14。下列原生地址属于本地 `AI6WIN.exe`，SHA-256：
`bb12a3faa7f2147b772a4e8d2235e815c3207e16c57b82882280bc08ce61f3a9`。

## 输入与复用边界

参考仓库提交：`ef076b486ad9a14052402cfa79d39178e7ecdf93`。
配置指定 `startup.mes`、`layer.arc`，640×480、CP932。归档目录与参考工程相同，图片为 AKB 而不是 RMT。

| 归档 | 条目数 | 类型 |
|---|---:|---|
| data.arc | 572 | AX / AREA / BIN / BMP |
| layer.arc | 2191 | AKB |
| mes.arc | 682 | 681 MES + liblary.lib |
| music.arc | 20 | WAV |
| effect.arc | 318 | WAV |
| voice.arc | 21374 | OGG |
| movie.arc | 566 | MOV / VSD |

全部 AKB 是独立 `AKB `，未发现 `AKB+`，当前解码器明确拒绝后者。
Flags 分布：`400000ff` 1803、`c00000ff` 276、`80000000` 89、`0` 22、`c0000000` 1。
24 位图取 flags 低8位作为常量 Alpha；32 位图始终使用文件 Alpha。最高位控制裁剪区外填充。
AKB 的行翻转发生在差分恢复之前，与参考 RMT 不同。

MUL 不带操作数字节的方言通过全部 681 MES 的指令边界和消息表检查。未修改原版字节码。

## 已核对的原生行为

### 变量容量

`startup.mes + 0xe1..0x105` 调用 `14/0`，栈底到顶：`100,15000,600,9192,0`。
容量为 bank1=100、raw=15000、word=600、byte=9192。将字节区底层上限扩为 16384，同时以脚本声明长度检查访问。

### 27/3：ShowWindow

`CFuncUI` vtable `0x545f7c`，调度 `0x4f3470`。
子调用 3 → `0x4f31c0` → 虚方法偏移 `0x20` → `0x4041f0` → IAT `0x5163a0` (`ShowWindow`)，参数 `5` (`SW_SHOW`)。
SDL 已创建可见窗口，因此此调用不再消费参数或写 VM 返回值。

### 31/10/0：消息区初始化

`CFuncExec` vtable `0x545e88`，调度 `0x4fd950`。
子调用 10 → `0x4fa9e0`；动作 0 → CMesWnd vtable `0x54732c` 偏移 `0xcc` → `0x481be0`。
设置系统变量 30=16、31=18、42=32、43=8、44=592、45=62、46=32、47=8、49=1。
该坐标是精灵内的文字区域；目前只验证初始化，尚未完成屏幕显示、确认、历史或保存语义。

### 原始变量和局部变量指令

原版字节码分派器在 `0x46f9f5`。`07/08/09` 分别通过 `0x505350/0x505270/0x505190` 读取 raw 区的字节、16 位、32 位视图；`11/12/13` 通过 `0x504d20/0x504c60/0x504ba0` 写入同一存储区。16/32 位索引以元素为单位，数据为小端序。实现增加范围检查，避免原版无边界访问造成内存破坏。

`10 → 0x504dd0` 写当前函数的局部参数；`0d → 0x505010` 对负数先钳制为零，再存入普通 16 位变量。raw 的 16 位写入不做该钳制。

### 19/7：图层矩形减暗

`CFuncLayer` 的 `0x4f6470 → 0x4f5da0 → CDIB+0x78 (0x48dd60)` 读取五个参数 `x,y,width,height,layerSelector`。宽度或高度非正时直接返回；合法矩形将目标图层 RGB 按原版逐字节减半，保留 Alpha。移植版按目标图层边界检查，越界显式报错；`layerSelector` 仍沿用 `0/-1/-2/正数图层` 的表面映射。原生在 selector 0 时还发送矩形失效通知，软件逐帧合成不需要单独的脏区队列。

### 19/4、19/5、19/6、19/8、19/9：图层像素操作

`19/4`（`0x4f68e0 → 4f5ef0 → CDIB+0x60/48e110 → 48c0c0`）实际读取十个参数，按 RGB 颜色键复制；按弹出顺序，第九个参数为透明色，第十个参数为 1 时连同源 Alpha 复制，否则只覆盖 RGB。早先记录将后二者写反，已纠正。`19/5`（`0x4f6720 → CDIB+0x4c`）读取八个参数，按源像素 Alpha 对 RGB 混合并保留目标 Alpha。两条路径都对重叠区域先取快照，避免软件实现受复制方向影响。

`19/6`（`0x4f6590 → CDIB+0x9c`）按颜色和 Alpha 标志填充矩形：标志为零且颜色为零时清零整像素；标志为零时保留原 Alpha，非零时写入颜色的完整四字节。`19/8`（`0x4f6280 → CDIB+0x58`）读取九个参数，按固定 Alpha 使用原版 `/255` 向下取整表混合 RGB，Alpha 255 复制四字节，非正值不操作。`19/9`（`0x4f6120 → CDIB+0xa4`）读取六个参数，只写矩形 Alpha。目标表面和坐标均保留边界检查，未知图层仍显式报错。

### 31/30/0：选择框初始化

`CNormalSelect` 虚表 `0x547714`；`0x4fb260 → 0x4f1a40 → 0x4effe0`。
分配两组各六个选项表面：两个 496×34、四个 496×52。将图层 5 顶部 320×68 的 Alpha 设为 255，保留 RGB，再用 8 像素端部和 30 段 16 像素中段生成五组 496×68 图块。
主题由 byte[1000] 选择；bank1[61] 为 1 时使用第一套。动作 1 通过 `0x4f0590` 打开选择框，选项脚本在独立 VM 中求值，文本固定按 CP932 绘制；确认后将原版返回值写入系统变量 18。

此接口不是标题，也不能复用 Kawa2 的 31/30。未知动作保留参数并报错；普通 `selparts` 变体和附录选择路线的其他资源动作继续保持显式边界。

### 31/520/7：独立动画管理器停止

`0x4fb980` 构造 `CFuncAnimeEx`，动作 7 经 `0x5032a0` 到 `CAnimeManagerEX +0x28 → 0x4dec50`，遍历 10×32 个轨道，最终 `0x405290` 将状态写为 255。

`opening.mes` 还会经 `liblary.lib` 调用 `31/520` 的动作 0、1、10 和 12：动作 0 按 `0x4fede0` 消费一个附加变体并加载 AX 资源；动作 1 按 `0x4fed70` 消费两个数值变体，经 `CAnimeManagerEX +0x14 → 0x4df540` 选择并重置后续解析所用的轨道读取器；动作 10 按 `0x503480 → 0x4de9a0` 将空闲轨道置为 pending 并同步事件；动作 12 按 `0x4fea10` 消费三个数值变体，保存目标图层并进入扩展轨道解析。命令字段为字符串时落入 `0x4fee60` 的默认返回。对原始函数 14 做静态 VM 执行时，已观察到动作 0 的 `z00.ax` 字符串变体、动作 1 的 `[0,0]` 和动作 12 的 `[3,0,0]` 栈形。运行时已接通资源、参数边界、轨道重置、空闲同步、首帧绘制、独立 20 ms 时钟，以及动作 3/4 的单轨道运行/停止和动作 6 的全轨道启动；扩展管理器构造器核对出的默认源层为 layer 8，31/520/12 的第三个变体选择目标图层。活动管理器的原生事件队列仍待补齐，动作 5/8/9/11 和活动事件相关路径继续保留参数报错。
实现单独的扩展动画状态，停止它不会影响 syscall 13 的普通动画。其余动作仍明确报错。

### 31/10/2：已隐藏的消息窗口

`0x481b00 → 0x4837a0` 在窗口不可见时跳过退场动画；已支持该启动状态。窗口可见时的消息精灵运动尚未接通。
消息区域 (32,8) 是消息精灵的局部文字坐标，不代表对话框在屏幕顶部；显示布局仍需核对。

`31/528` action 9 的旧无操作映射已撤回。`4fa9e0` 实际属于 `31/10`；`31/528` 经 `4fdbf2 -> 4fc080`，动作 9 的压缩跳转表 `4fcc99=1 -> [4fcc64]=4fc192` 进入参数变化动画，调用 `4a00f0`。当前完整保留参数并明确报错，不跳过。

### 媒体初始状态与背景名称

`31/1010` 的 `0x486160` 在尚未选中媒体流时直接返回；只实现此初始分支。
`31/1011` 动作 2/10 经 `0x503cd0/0x503d30` 清空或设置背景资源名，按原程序转为 ASCII 大写，长度受限。
`31/528/23` 经 `0x4fc811 → 0x4b2fa0(0)` 隐藏辅助窗口；目前仅支持辅助界面已经隐藏的状态。

### 日文版进度存档

`14/11` 经 `0x4f7ab0 → 0x5080a0`，使用当前 bank1[61] 选择规则。汇编的 ECX 已核对源和目标：

- 常规模式：3570..3594 仅当当前 byte[3570] 非零时整体覆盖；3290..3599 的其余部分仅写入非零值；其他字节取无符号最大值。
- 另一模式：3650..3711 无条件覆盖；其他字节取无符号最大值。
- 两种模式都只对 word[32..99] 取无符号最大值，其余字段保留。
- `14/3..6` 经 `0x4f8ad0/0x4f88a0/0x4f8670/0x4f8440`，按相同下标恢复 byte/word/raw/bank1；读写失败保留调用参数。
- 序列化允许 9192 字节变量及 15000 字节 raw 区，保存仍使用临时文件替换。

首次使用缺失槽时，依据原版 `FLAGINI.MES` / `HAGE_FLAGINI.MES` 重建初始化状态：槽 0..100 / 101..201、51 个系统变量、9192 bytes、600 words、100 个 bank1、15000 raw、byte[4008]=1、后一组 bank1[61]=1。只在移植版独立存档目录写入；原目录已有进度没有导入。损坏文件明确报错，不退回空白存档。

### 31/612/2：CBowling 资源释放

`0x4fb5a0 → CBowling` 次级虚表 `0x54519c +0x10 → 0x4cfd20`，次级 this 偏移 0x9c。实现位于 `runtime/bowling.h`，显式调用和运行时销毁共用释放路径。

- 次级偏移 0x3c34..0x3ce4 共 45 槽，释放其中 39 个自有对象，保留 0x3c78..0x3c8c 的六个借用图层；保留原版 0x3cc8 先于 0x3cc4 的析构顺序。
- 对 0x3d14 的对象列表执行注销、移除、析构；通过所有权析构和注销回调对应原版虚函数及 `0x4e21b0`。
- `0x40cdb0(0)` 清空当前保龄球对象指针，再清零 0x3cec 状态。创建前空状态与重复释放均有效。
- 所有权信息不完整时，先报错且不部分释放；调用参数不消费。创建/运行的动作 0/1 继续明确报错。

验证包括实际堆分配的 39 个资源、六个借用槽、登记对象注销顺序、空对象、错误所有权、重复调用以及运行时退出清理；独立测试通过 ASan/UBSan。真实脚本验证了创建前的释放路径，保龄球玩法和完整资源创建尚未接入。

### 当前停止点

启动执行 160 次调用、481 帧，到达 `liblary.lib+0x24e` 的日文标题菜单；主机标题渲染和交互测试通过。新游戏探针（只运行移植 VM，不启动 PC 程序）撤回错误的 `31/528/9` 空操作后，当前在 772 次调用、1168 帧处停于 `liblary.lib+0x6949`。之前返回标题的结果越过了该错误空操作，不能作为正确剧情流程证据。此前静态/主机路径曾推进到 `opening.mes+0x4beb` 的 `31/524/0`，随后静态核对到 `liblary.lib+0x47bf` 的 `31/0`，这些接口均已接入；完整剧情仍需继续核对后续脚本路径，不能视为可通关。

- `31/1011/0,1`：504420 媒体表初始化；1554 条常规记录、72 条备用记录、189 条链接。保留重复文件名条件，备用模式保留已有链接表。构建工具校验原 EXE 哈希，仅静态提取元数据。
- `31/10/3`：481df0 消息窗口皮肤；六个按钮裁剪、640×84 背景、原版配置颜色换算。消息文字区域、渐显、自动确认和历史记录已接入，窗口可见时的原生精灵运动仍待补齐。
- `31/110` mode 0：4ed590/4ee6b0 初始布局和主菜单，依进度禁用条目，五或六个菜单项，结果写系统变量 18。按下/松开动画、音效及其他模式未完成；title.area 当前仅检查存在，布局使用已核对原生坐标。
- `31/528/24`：4beab0 → 4bdc80/4bdd40/4be480。清空日记两组 96 项，重绘 diary.akb 到 608×1936 私有位图，恢复一天、96 像素滚动范围。日记窗口显示与编辑接口未接通。
- `31/528/10/0`：49fc40/49e250/49d9c0，四次 49f5a0 更新。参数窗口前三行的数值条及绿色透明数字重绘，保留数字后方像素；第四项按 49e110 在隐藏 602×112 DIB 的 (166,92) 绘制两格数字，并保留小于 10 时原版不覆盖十位的行为。49dee0/49dc60 在 (474,92)/(534,92) 绘制总数和剩余数，分别保留 >=100 特殊字形及剩余数为 0 时只写第二格的行为；param.akb 为 604×160，原版先分配 604×260，下方是运行时暂存区。`31/528/25,26` 的特殊标记设置按 49ebb0/49eb60 实现，坐标来自 5134d0/5135e0 的动态初始化，范围分别为 0..8 和 0..6。

CLetter `31/525/2` 已按 `0x48adf0/0x48a960` 实现私有表面保存与切换、三档淡化、16 像素字体尺寸及过渡结束后的消息窗口重置。`0x48c390/0x48eb80` 确认两项乘积分别除以 255 取整，RGB 混色不修改中间帧 Alpha；最后一帧整幅复制。原版每步等待 15ms，当前依托运行时 60Hz 帧驱动。文字模式进入和文字显示动作尚未支持。

`31/524` 已按 `0x4f9eb0 → 0x45c940/0x45c8e0` 接通动作 0/1：从 `timepart.akb` 对应的 layer 7（640×400）按原版固定坐标绘制绿色键控的 120×128 头部和三段状态图，动作 1 恢复绘制前的场景表面。动作 29/30 继续使用独立的四组精灵可见状态快照；未知动作保留参数并报错。

### 31/40：CFuncExec 垂直翻页

`0x4fd1d0` 使用 layer 2 的 640×960 双页源和 layer 0 的 640×480 目标。方向 0 从上页向下推入，非零方向从下页向上推入；`Display/EffectSpeed` 0/1/2 对应 8/32/64 像素步长，越界值回退到 8。每帧执行一次窗口复制，结束帧按原版再做一次整页复制。

### 31/523：Sungeki 六步板面绘制

`0x4fce50` 读取一个模式变体，模式 0 调用 `0x508b70`，模式 1 调用 `0x508620`；其他模式保留参数并显式报错。运行时实现位于 `runtime/animation523.inc`，逐项复现两条原生绘制链：layer 3 的 576×196 板面自复制、layer 7 的 `0xc0/0xc8/0xd0` 绿色色键图块、layer 2 的 6 像素行拷贝，以及 layer 0 在 `(0x20,0x5c)` 的呈现。普通复制保留原版 copy-alpha 标记，色键复制使用 `0xff00` 并保留目标 Alpha；重叠复制先快照源矩形。原生每步的 20 ms 等待由主机同步完成六步，避免阻塞 VM；缺失或尺寸不足的图层直接报错，调用参数保持不消费。

### 31/521：双页图层蓝色遮罩转 Alpha

`0x4fa060` 读取一个图层选择值，经 `0x503100/0x502c00` 取得 CDIB 后调用 `CDIB::virtual_180 (0x48cc20)`，固定处理 `(0,0,0x280,0x1e0)`。该虚函数从同一 640×960 双页图层的下页读取蓝色通道，将 `((31-(blue>>3))*8)` 写入上页 Alpha；原版把 248 特判为 255，BGR 保持不变。运行时实现位于 `runtime/animation521.inc`，按 KImage 的 top-down 布局读取 y+480 并写入 y=0..479；非法图层、尺寸不足或非数值参数均显式报错且保留调用栈。

`exec-dispatch.json` 为扩展分支地址表，不代表已实现。`native-classes.json` 收录 181 个类的虚表和方法地址；`native-rtti.json` 为各 CFunc 入口。

### 31/522：Sungeki 四个窗口精灵

`0x4fcef0` 的 0..5 模式已按 `0x46cad0/0x46d350/0x46c050/0x46c8f0/0x46c760/0x46cee0` 接通。它们操作的是 `0x40c5d0` 返回的四个独立窗口 DIB，不是脚本 layer；运行时在 `runtime/animation522.inc` 保存四个 108×124 BGRA 精灵、位置和背景快照，按原版 `0xff00` 色键及 Alpha 复制更新 layer 7 图集，并在主机帧开始恢复背景、结束时合成可见精灵。模式 0/1/2/3 分别消费一个选择值，模式 4/5 分别消费两个选择值；模式 1 的 20 ms 移动循环用原版确定的最终位置（`0xac + index*0x78, 0x10`）完成；模式 2 仅更新原生状态，模式 5 隐藏并清理四个窗口。模式 3 的 0 或大于 3、模式 4 的第一选择为原版无绘制返回；其余参数和缺失图集/当前精灵状态均校验并保留参数报错。

## 静态反编译

按用户要求不再运行原版 PC 程序。使用本地构建的 r2ghidra 6.2.2 与 radare2 6.2.0，识别并导出 **5,307 个函数的伪代码**，结果在 `local/decompiled/`。其中 104 个函数经隔离重分析恢复，最后两个关闭不可靠的局部变量推断后恢复。工具源码只在 `local/`，没有链接进 Switch 程序。

所有已识别函数都有非空输出，不等于识别了全部可执行代码，也不等于恢复了原始源代码。变量类型、间接调用和控制流仍需汇编核对。导出的伪代码不是可以直接编译运行的移植结果。摘要见 `native-decompilation.json`。

可重复入口：`tools/decompile_r2.py`（pefile、r2pipe）；备用 Ghidra Headless 导出入口为 `tools/decompile-native.sh`。

## 验证

- macOS C11 主机编译、devkitA64/libnx Switch 编译通过（警告作为错误）。
- AKB 方向、裁剪、Alpha、背景、截断数据、越界与签名测试通过 ASan/UBSan。
- 2191 张图像 C 解码成功；681 MES 结构扫描成功；51 个库函数注册。
- 真实启动回归检查容量、文字区坐标、图层、FLAG100 恢复、初始备用模式及未支持接口的参数保留。
- FLAG 区间边界、覆盖条件、无符号数值、损坏输入及完整序列化通过单元测试与 ASan/UBSan。
- SDL 无显示设备模式：标题截图已检查，日文标题启动通过；菜单返回值、禁用项、媒体表重复键、皮肤裁剪、日记重绘及参数数字有集成测试。

没有进行剧情、音视频效果、存读档、小游戏、路线结束或 Switch 实机验证。容器类型可识别不代表播放功能已验证。

## 后续顺序

1. 继续追踪新游戏路径的未实现接口；禁止用未知调用的空实现推进。
2. 补齐 CTitle 其他模式、按键按下/松开状态与音效。
3. 建立鬼作独立消息、选项和进度适配。继承的 Kawa2 gallery、scene、save 数据常量不可沿用。
4. 接入全部可达的玩法及持久化，再做路线与实机回归。

### 2026-09-20：CMesFadeSprite 生命周期与参数字形纠正

- `31/43` 经 `4f95a0 -> 4294c0` 取管理对象 +0x44，虚表入口 `485f40` 从 VM 取动作；它不是析构调用。动作 0/1/2 分别对应 `485e20` / `4852a0` / `4853e0`。
- 已实现三个独立 640×480 私有表面、重复初始化/释放、第三表面 BGRA `0x64000000`、动作 2 的透明清屏与系统变量 42..45 重置为 0,0,640,480。精灵构造参数 5/4/3 是排序字段（`46bb50` 写 +4），不借用同号 VM 图层。当前可显示的私有精灵仅为全透明；动作 3..6 的实际过渡尚未接入，必须保留参数报错。
- 原版字形表 `5352f8` 是 524 起、步长 8，目标坐标表 `535368` 为 (166,92)、(474,92)、(534,92)。纠正剩余次数偏移 8 像素；零值写第二格 x=542。恢复曾被提前 return 跳过的逐 BGRA 字节测试，撤回无原版依据的透明过滤。计数更新调用 `49e250` 恢复背景，零值第一格因此应保留恢复后的背景。
- `build-host.sh`、`test-host.sh` 通过，包含新增生命周期、非法目标不修改状态、未知动作保留栈、字形逐像素断言。探针日志在 `local/new-game-message-fade.log`；探针结果不代表完整剧情或 Switch 实机验证。

### 2026-09-20：CMesFadeSprite 消息过渡

- 继上节生命周期实现，新增 `31/43/3` 的 type=1：消费 duration、一个原版已读取但未使用的数值、type。`4858c0` 的复制源为系统变量 49 指定图层，源 x=0、y=变量47，目标 x=(640-变量46)/2、y=变量47，宽=变量46+2、高=变量31。先复制至私有 CFadeSprite，再令旧消息 255→0、新消息 0→255；结束复制至持久消息表面并清空临时表面。type=2/3 尚未实现，明确保留全部参数报错。
- 动作 4/5/6 按 `485790` / `4856a0` / `485560` 实现消息淡出、遮罩淡入/淡出。已经显示的遮罩重复淡入、已隐藏遮罩重复淡出只消费参数。
- `46bbe0` 在 flag 0x4000 未设置时，把步数依 EffectSpeed 0/1/2 改为 duration/2、duration/4、0；该标记设置时保留原步数并禁止点击跳过。`46bca0` 每轮 15ms，移植用 60Hz 帧累积 1000/900 推进虚拟 tick。`46b5d0` 整数插值，结束与跳过均按 `46b530` 落到终点。
- `427770` 按优先级升序排列；三个私有精灵按 3/4/5 合成。`46ab10 -> CDIB+5c -> 48c200` 先计算 sourceAlpha*opacity/255，RGB 分别取整再相加，目标 Alpha 不修改。增加独立底图恢复，VM/帧更新后重绘，避免持久精灵逐帧叠加。
- 新增合成像素、中间帧、源矩形、三档速度、15ms 时钟、VM 等待、点击跳过、flag 0x4000、参数保留及遮罩状态测试；`build-host.sh`、`test-host.sh` 通过，Switch 交叉编译通过。
- 默认 explore 探针返回标题，无未知接口错误（`local/new-game-message-transition.log`）。first 路径执行 8942 调用、1610 次文本后达到帧预算，last 路径 4306 调用、678 次文本后达到预算；都停在消息等待 `liblary.lib+0x47bf`。栈分别增长至 1609/677，必须继续核对循环与栈增长，不能作为剧情正常的证据。日志分别为 `local/new-game-message-first.log`、`local/new-game-message-last.log`。

### 2026-09-21：操作数栈与 31/528/9 映射审计

- `31/0,1` 原版 `4fabb0/4fab00` 将消息返回值交给 `4b7120 -> 46f390`，后者调用 `402720` 追加 variant；不足容量时 `402920 -> 402c30` 按约 1.5 倍扩容。`4e4840` 中 opcode 1 的异常分支清理局部参数对象、恢复调用位置，没有统一删除操作数栈的消息返回值。移植不以清栈消除增长，改为可扩容 `KValue` 存储，分配失败保留原数据并显式报错。
- 新增超过 10000 项的整数/借用字符串混合、扩容时以栈中值作为 push 参数、完整 LIFO 验证。主机回归通过。旧映射下长探针达到栈深 4851、容量 5530；这只能验证不再触发移植人为的 4096 项限制。前 20 次选择日志显示强制 first 路径重复选择 `opening.mes+0x1d34` 已访问项，不能据此认定 VM 死循环。
- **纠正前述运行记录**：此前把 `31/528/9` 错映射为 `4fa9e0` 的 default，实际 `4fa9e0` 是 `31/10`。真正 `4fc080` case 9 消费七个参数，调用 `4a00f0`，包含参数插值、窗口显示、AX 轨道 0/1/2 与音效等待。错误的无操作处理已移除；测试现在验证全部十个输入栈值保持、handled 不增加、明确报错。
- 本轮可信的新游戏结果：772 次调用、1168 帧、63 次文本，停在 `liblary.lib+0x6949` 的 `31/528/9`，原输入完整保留。顶部七个参数（动作以下弹出顺序）为 `100,1005,1000,1000,1000,0,1`。其余前置值不应自动清除。此前 3417/3509 调用返回标题和长循环统计经过了错误空操作，不能视为正确剧情验证。
- 下一步：`49d680` 计算四项目标（1000 为不变、两侧为增减量，前三项上限999、第四项80），`4a00f0` 结合 AX 状态逐步显示，再落到最终值并处理音效。未接通同步前继续报错。诊断程序帧数耗尽现明确输出“not a pass”，不再留下空白结论。

- 本轮验证：主机完整回归通过；操作数栈单元测试额外经 AddressSanitizer/UndefinedBehaviorSanitizer 运行通过；macOS 与 Switch 重新编译成功。交付程序同步后仍是开发预览，新游戏会明确停在待实现的参数动画，而非跳过它。

### 2026-09-21：参数动画的数值与 AX 基础

- `runtime/param_change.h` 实现 `49d680` 的目标计算：汇编 `movzx` 证实输入是低16位无符号；常量 `5489a0` 为 double 1000。编码1000保持原值，较大值增加差量，较小值减去差量；前三项限0..999、第四项限0..80。浮点步长为钳制后的距离/低16位步数；零步数仅保留最终值，不能做插值。`4a00f0` 的总数最终为原总数加第四项目标差量，按16位回绕。未将这些目标提前写回游戏状态。
- `ax_count_boundaries` 对应 `4dd8e0`：从第0轨道起始位置扫描，计数 opcode1，执行两个循环槽，opcode3返回-1、4/255返回计数。失败不改变计数输出或播放状态；恶意或无终止循环显式失败。实际 `act15.ax` 第0轨道计数59，原版会覆盖调用参数100；这不等于动画已经播放。
- `ax_tick_native` 对应 `4decb0` 的事件结果2..11，区分边界、普通延迟、回跳、停止、循环、文件结束与绘制。opcode1边界延迟是max(arg-1,0)，opcode2使用完整arg，延迟期间事件值保持。保留独立入口，尚未切换现有全部播放路径，也未声称原生动画队列已实现。
- 动作9预检查七个数值参数、AX程序和初值，计算计划后仍明确报错、保留全部输入。后续必须处理 `411c30` 返回的 CAnimeManager（引擎+88，构造器4de390）的轨道0/1/2队列、参数窗口可见生命周期、20ms泵及音效等待，之后才能提交计划并恢复VM。
- 新增 `tests/param_change_test.c` 并接入主机构建/测试，覆盖低16位参数、上下限、总数回绕、失败不改输出、双循环计数、结束/回跳、损坏输入和无限循环、原生事件与延迟。完整主机回归通过，新游戏仍在772调用的原卡点明确停止。

- 补充验证：数值与 AX 单元测试经 AddressSanitizer/UndefinedBehaviorSanitizer 通过；Switch 交叉编译通过。交付同步本轮构建，仍为开发预览。

### 2026-09-21：31/528/9 参数演出接通

- `4a00f0` 已接入运行时：按原版栈顺序读取 duration、四个低16位编码、chime 和 track2 标志，优先采用第0轨实际 59 个边界；每个 20 ms AX 边界按原版 float 累积、整数截断和第四项总数增量更新参数窗口。
- 接通 CAnimeManager 轨道 0/2 重置、启动、边界事件、结束停止；IsSungekiSE 分支只等待原版要求的轨道，chime 使用 `ti-n.wav` / `ti-n2.wav`。参数窗口保存并恢复底图，临时窗口按 bank1[61] 控制可见性，确认键可按原版直接落到终值。
- 实际日文新游戏探针已从 772 次调用的旧卡点继续执行到 3417 次调用、2733 帧、212 条文本并返回标题；参数从 800 更新到 805，轨道状态为 255，操作数栈回到 0。该结果只证明启动路径继续运行，不等于完整可玩性。
- 主机完整回归、资源审计、Switch 交叉编译通过；交付包已同步，最新 `kisaku.nro` SHA-256 为 `fca4aac9866c0a38ea48115590083041cbf08981cf95c731ba4058c90374f0f4`。
- 探针的 `first/last` 策略已改为优先选择未访问项，避免诊断工具重复选择同一选项造成假性脚本循环。修正后日文路径 3417 次调用、2733 帧、212 条文本、11 次选择即可回到标题，操作数栈为 0；此前 8942 次调用和 1609 项栈深是探针选择策略造成的假象。

### 2026-09-21：开场动画与 Switch 进入游戏崩溃审计

- 按《鬼作》`CAnimeManager` 的 `4de390/4de760/4de650/4decb0` 重新核对开场和剧情 AX：普通动画源层固定为 layer 8，目标为 layer 0；kind 0 是 BGRA/RGB 复制，kind 1 使用 `0xff00` 色键并把第二页 Y 坐标减 480，kind 2/3 是空实现。旧的 layer 5 + layer 4/2 合成链属于参考游戏，已从鬼作普通 AX 路径移除。
- AX 播放切换到原版事件入口：opcode 1 的边界延迟为 `arg-1`，opcode 2 保留完整延迟；普通和扩展管理器仅在原版系统标志 `0x10` 有效时推进。增加 layer 8、RGB/色键、第二页 Y、标志门控和事件时序回归。
- 静态检查发现进入第一段消息时 `message_text` 只分配 560×54，却按鬼作原版区域读取 576×54，Switch 会在选择开始游戏后立即触发越界并退出。已按 `32..608` 的原版区域改为 576×54，并将清空改为按实际 stride；主机启动经 ASan/UBSan 不再报告越界，完整回归通过。
- 选择项求值器现在拥有独立的动态操作数栈，避免 1.5 倍扩容覆盖调用方栈；拒绝的脚本仍保留调用方参数并明确报错。
- Switch 交叉编译和交付同步完成。当前 `build-switch/kisaku.nro`、`交付/SD卡根目录/switch/kisaku/kisaku.nro`、`kisaku-preview.nro` SHA-256 均为 `22c822bb03fcb5cadcb4f3ca642159547a47283ca5cec8a134995e9aecb353f2`。由于没有 Switch 崩溃转储，实机结果仍需以这版运行后的 `saves/runtime-error.txt` 或系统错误记录继续对照；代码已不再把这条路径静默跳过。

### 2026-09-21：消息框、选择框与开场音量

- 之前 `31/10/3` 虽然解码了鬼作皮肤，实际消息合成器仍读取参考游戏的图集和八按钮布局。本轮将 `kisaku_DL_mes_p.akb` 接入显示：背景 `(0,396,640,84)`，文字起点 `(32,404)`，六个独立按钮位于 `y=464`，包括系统展开、游戏结束、回看、跳过、自动和输入标记切换。按 `481df0/482450/484270` 使用图集状态与坐标；指针和方向键使用相同按钮映射，设置生效后刷新背景颜色。游戏结束按钮的实际动作在下述第二轮修复中纠正。
- 进一步核对 `481be0` 汇编：`560×54` 是排版区域，`481d68/481dca` 实际清空的是两个完整 `640×84` 文字表面。修正只清局部导致右侧旧图像在完整显示文字时被复制出来的问题；保留已有 `576×54` 缓冲区，避免重新引入溢出。`local/message-fixed.png` 为 macOS 移植运行截图。
- `CNormalSelect` 按 `4f1890/4effe0/4f0970` 每次打开重建主题，34 像素底板按 `8+18+18+8` 拉伸为 52 像素行；普通状态红色标记替换为 `SELECT/CnR,CnG,CnB` 与 `MIXED` 配置，绿色透明。末页不足四项时保持原 top；原生 ▲/▼ 行替换临时页码栏。按 `4f0910/4f1700` 恢复内侧热区和禁用项判断，禁用项不会提交返回值。文本保持日文 CP932。
- `464610/464680/4646f0` 的音量范围为 0..104，DirectSound 百分之一 dB 值为 `-((204-v)*(104-v))/10`。移除语音/音效沿用的 `(v-255)*18` 换算：原 INI 音量83从约2.8%增益修正为约74.6%。设置滑块与触摸映射同时使用104上限，历史语音采用相同换算。
- 新增实际启动资源验证：`logo.wav` 和 `potapota.wav` 的 PCM 逐样本符合原版增益，启动混音输出211200字节，其中162108字节非零。此证据验证软件输出，不代替 Switch 扬声器实测。
- 主机回归、新增窗口像素/点击/禁用项/翻页测试、ASan/UBSan 和 Switch 构建通过。全新存档 first 探针达到3417次调用、2733帧、212次文本、11次选择并返回标题，栈为0（`local/ui-fixed-fresh-probe.log`）。已有 `local/saves` 的同策略探针耗尽20000帧，不能当作通过记录；诊断需保留存档条件。
- 本轮构建 `kisaku.nro` SHA-256：`c6d1911b7ff1656a345ab0b121ab4f957b1c86f2d68a9391c7c76c28ea6f9ff9`。仍是开发预览：完整路线、Switch 实机、系统菜单原生对话框、全部消息动画时序及其他未支持接口尚未完成验证或实现，不能声称窗口与演出全部还原。

### 2026-09-21：AKB 透明度、消息时序与原版确认框

- `4df9e0` 证实 AKB 标志 `0x80000000` 控制裁剪区外的背景填充，不能用它判断是否存在 Alpha；`0x40000000` 才区分24/32位。32位总是保留第四通道，24位使用 flags 低8位作为常量 Alpha。原解码器把 flags=0 的32位界面素材强制成不透明，导致按钮边角和标签底色异常。已修复并增加合成资源及真实 `kisaku_DL_dialog_p.akb` 的透明/半透明像素断言；全2191张AKB解码通过。裁剪区外“不填充”对于已有目标表面的语义仍待另行审计，不据此声称所有加载覆盖行为均已还原。
- `484b22..484b81` 消息逐字延迟改为原版 double 二次式 `trunc(250 - (0.01365*speed*speed + speed))`；默认86，只有124直接显示全文。旧便携设置125..255兼容归一为124。首字立即就绪，换行不重置逐字计时。`465300` 自动翻页间隔按累计消息字节数计算 `500 + trunc((104-speed)*1.7)*(bytes/2)`，默认速度52；时钟从消息开始、包含窗口和逐字演出时间。
- 消息框显示/隐藏按 `46bbe0/46bca0/46b5d0` 使用独立15ms时钟，EffectSpeed 0/1/2 将12步分别变为6/3/0；标志0x4000保留12步并禁止按键跳过。跳过窗口演出不会同时消费对白；隐藏前完成文本复制。`EnableOpen` 持久化，并按原版禁止在已读快进时折叠菜单。
- 按 `484d10 -> 4b4060` 将原版“ゲーム終了”按钮从错误的设置动作8改为退出确认动作6。`CDialog 4bf530/4bfaa0` 的退出/返回标题确认框使用原版428×88框体、标签、两按钮和 `dialog.area` 热区；按 `48df30 -> 48bda0` 合成直通 BGRA，保留中间 Alpha 和各项除255分别取整。初始无默认选项，取消/No不退出，Yes保存进度后退出；保存失败保持对话框并显示错误。鼠标、键盘、手柄方向映射和触摸共用原生选择语义，长按取消、跨弹窗触摸不误确认。
- 新增 `tests/message_panel_test.c` 验证实际前端渲染、无默认Yes、指针/方向/触摸确认取消、触摸归属和失败保存分支。完整主机回归、前端专项与 ASan/UBSan 通过。截图：`local/dialog-fixed.png`、`local/message-timing-fixed.png`；均来自移植程序，没有运行PC原版。新建 `local/window-final-fresh-saves` 的探针记录于 `local/window-final-fresh-probe.log`：3417次调用、2441帧、212条文本、11次选择，操作数栈0，回到标题；帧数变化源于原版消息时序修正，不代表完整路线通关。
- Switch 构建通过，最新 `kisaku.nro` SHA-256：`a13529cb5fba487bc071e1919dad846a90793f7b0511f85d70d2a0f94f6ef9b9`。仍为开发预览，不能替代实机验证。

#### 已确认的剩余缺口

- 此轮审计发现设置、回看、存读档/导航仍有参考游戏专用实现。其中设置/回看已在下述后续实现中替换；存读档/导航尚未完成。
- `runtime/save_runtime.inc` 保存门槛及读取校验仍要求8192字节，鬼作VM实际为9192；元数据偏移、场景地图、动画脚本恢复也沿用参考游戏。存读档当前未完成，不能仅修改长度常量后宣布可用。
- 消息系统菜单四按钮展开/收起的错峰运动（483d10/483b80）、姓名前缀立即显示（482b20）及其他消息模式仍待补齐。此次独立15ms时钟只覆盖普通消息框显示/隐藏，不涵盖所有演出。
- `31/43` type2/3、`31/525` 其余动作、`31/612` 动作0/1、`31/520` 部分原生队列接口仍保留参数并明确报错。完整路线、回想、存读档往返和 Switch 实机闪退/音频需继续验证。

### 2026-09-21：原版设置页面与实时音量

- 按 `4c8ed0/4c47c0/4c6e00` 重建 CConfig：使用五张 `kisaku_DL_text/sound/display/sound_p2/sound_p3` AKB 图集与 `config_00/01/02/01_2.area` 的实际22/27/26/68个区域。实现文字、声音、显示三页和33名角色语音子页，原版标签、坐标、按钮覆盖顺序、禁用状态及侧栏后三项。设置不再请求参考游戏的 `option1_p.rmt/option2_p.rmt`。
- 设置保持草稿，決定后事务写入，取消恢复；角色子页取消恢复进入时的33项快照。默认值按 `463fe0`，显示限制依据系统变量61和byte4007；删除错误的byte8190写入与参考动画切换。CONFIG/Page保留，但重开时角色子页归一到声音页。原版RGB/Alpha范围0..224、文字速度0..124、声音/自动速度0..104；设置预览按 `4c1380` 使用255/224，消息皮肤的 `481df0` 仍使用255/184，两者不能混为一谈。
- `runtime/voice_character.h` 按 `464140/466630` 处理17个字母前缀及NPC n001..n343的16段映射，总计33项 `IsCharVoice00..32`；字面量 `no-` 绕过角色过滤。按 `4b6350` 保留静音台词的历史语音名，回看重播按 `4d9d30` 同样检查全局及角色开关。旧的男女文件名猜测已移除。
- 修复解码时直接缩放PCM导致当前音乐无法恢复音量的问题：游戏BGM、语音、音效保留原始PCM，在输出混音时应用当前设置。主音效和channel3分别使用 `IsEffect/Volume` 与 `IsHEffect/HVolume`（`4b62b0 -> 465090/4653d0`）。渐变保留独立衰减量，音量和静音设置不再破坏样本。已排入SDL队列的短尾仍可能保留旧音量。
- 实际logo WAV验证更新为原始PCM逐样本一致并检查软件混音非零；新测试验证BGM和语音静音后恢复时样本不丢失、普通/H音效独立增益、全部角色与NPC段边界。配置测试覆盖实际热区边界、拖动、禁用项、取消/子页回滚、持久化与写入失败不修改实时设置。`local/config-regression.log`、`local/config-bootstrap-asan.log`、`local/config-panel-asan.log` 均通过。
- 截图来自移植程序：`local/config-text.png`、`local/config-sound.png`、`local/config-display.png`、`local/config-voices.png`。设置进入/退出及切页滑动、音量滑块试听尚未还原；手柄采用方向选取控件，未复刻Windows指针跳转。MovieVSync配置已保存，不能据此声称复刻Direct3D呈现行为。

### 2026-09-21：原版回看窗口

- `4da380/4d8450/4d91d0/4d9400` 对应 `kisaku_DL_backlog_p.akb` 与九区域 `kisaku_DL_backlog.area`，已替换不存在的 `baklog.rmt`。背景为Alpha192黑色，原版616像素分隔线/选中框、右侧24像素栏、16×20箭头和滑块按各自图集状态合成；文字从x32、每行y+10开始，16像素字形、18像素行高、560×54排版区域，使用已有日文禁则排版。
- 历史最新项固定第六行，少于六项保留顶部空行；offset按单条移动，上限max(count−6,0)。滑块按 `4d8720/4d9b00` 使用412像素行程及四舍五入映射。点击整条有语音记录的区域重播，空行和无语音行不进入虚构的全文子页。恢复 `4d8cb0` 方向焦点关系，接入鼠标悬停/滚轮/拖动、手柄、触摸；向最新记录之外滚轮滚动关闭回看，首次取消中断语音，随后取消关闭。
- 新前端测试覆盖空历史、两条底部对齐、64条环形索引、热区边界、单条/快捷翻页、滑块极值/中点、离开滑道后的触摸归属、真实 `a0001.ogg` 解码和角色静音。截图 `local/backlog-partial.png`、`local/backlog-full.png` 使用非剧情测试文字；仅为移植界面验证。回看仍基于当前64条扁平文本/单语音记录，原版命令记录、多语音序列及文字阴影状态尚未完整恢复，不等于完整CBackLog执行器。
- 最新主机构建、完整回归（`local/backlog-regression.log`）、前端ASan/UBSan（`local/backlog-panel-asan.log`）及Switch编译通过。主机SDL2-compat的ASan运行需 `DYLD_LIBRARY_PATH=/opt/homebrew/opt/sdl3/lib`，否则会在SDL库初始化前弹出缺少SDL3的框，并非游戏内崩溃。
- 全新 `local/config-final-fresh-saves` 探针：3417次调用、2441帧、212条文本、11次选择、栈0，回到标题（`local/config-final-fresh-probe.log`）；仍不代表完整路线通关。最新 `kisaku.nro` SHA-256：`c3abe2e2558690860e07b859f4250ed5e8654d6a80bd4d6016b275c84aed2a7b`。
- 存读档的9192字节状态、鬼作元数据和场景恢复、其余未实现原生接口及完整路线/实机仍是后续工作。本轮没有运行PC EXE，没有修改原始输入。

### 2026-09-21：Switch 画面白点与绿色块

- 用户照片对应 `ev01.akb`。核对完整调用链 `4f68e0 → 4f5ef0 → 48e110 → 48c0c0` 及汇编，19/4弹出顺序最后两个参数为RGB透明色、copyAlpha；旧实现写反。开场实际 `liblary.lib+0x2eb/+0x32b4` 调用为透明色0x00ff00、copyAlpha=1，旧实现因此错误跳过0x000001，且把绿色画进目标。
- `ev01.akb` 有49个0x000001像素，主要位于暗色轮廓；已校正参数位置。真实`b00an.akb`有135144个绿色键像素，回归逐像素验证这些位置保留底图，非键RGB正确覆盖，并分别验证copyAlpha=0/1。测试同时检查ev01全部像素，49个近黑像素不可丢弃。此问题是颜色键参数错误，不是应当用滤镜消除的素材噪点。
- 对2191张素材重新统计：所有裁剪不是整图的366张都带0x80000000填充位；因此通用“无填充的局部AKB覆盖”偏差不解释本次原始素材现象，仍留作独立兼容性待办，没有把猜测当成已复现原因。
- 主机完整回归、运行时ASan/UBSan、Switch构建通过；已同步交付主NRO和兼容名。SHA-256 `f00feba61bb2bda7a1cfea427afb095e318087986c118dfd34fd8a346762ea0a`。没有接入Switch实机复测，不把编译/软件像素断言写成实机通过。

### 2026-09-21：存读档基础修正（继续实现中）

- 保存与恢复接受原版9192 byte /600 word /15000 raw；删除参考游戏map.map、场景数组覆盖及byte8190/8191的_m脚本改名。脚本名按4e2b50大写并将.ADV转.MES。bank1[61]选择普通/另一模式命名空间，不再借用byte8100。
- 478240的元数据位于raw+1002，每槽45字节：present、stage、sys7/100、六个16位时间字段、28字节CP932备注；raw+1000保存0..99槽号。写入后立即更新VM借用raw指针。备注超过27字节拒绝，保存失败不替换已提交槽。
- 实际新游戏前三个检查点分别保存，在全新运行时从标题读取，通过原版MES恢复前导重建背景并继续下一句；可见RGB逐像素一致。屏幕Alpha是私有呈现状态，前端按不透明输出，所以不把其未使用字节作为画面差异。
- 恢复前导暴露31/526/1旧实现多取参数：4fd915直接调用47ae90，没有附加变体，现已纠正且测试保留调用方栈。动作2实际调用47b380地点标签，已撤回错误的CLetter淡化映射，后续实现见下。
- 仍待：跨槽全局进度合并、参数/日记私有窗口恢复、更后期/多层脚本/全部路线读档。前三个检查点通过不代表完整存读档完成。

### 2026-09-21：地点标签与存读档主界面

- `31/526/2 → 47b380 → 47b220/47b020` 使用bank0[0]索引523808处40个32字节CP932地点名。与媒体表一起按已知EXE哈希静态提取，生成头不提交。layer7在(160,336)构造背景，两私有336×32精灵按背景/文字顺序显示在(608−8×编码字节数,16)，绿色键透明，红色部分按Msg配置255/184换色与Alpha。标签自身32像素高，越出640右缘部分裁剪，混合保持目标Alpha；背景仅保存覆盖矩形，避免恢复时擦除消息区的新内容。动作1释放幂等，动作0仍保留参数报错。
- 40个地点逐一验证日文解码/字形、左右帽与绿色键、背景Alpha、位置与剪裁、重复帧不积累混色、释放恢复背景及区域外更新不丢失。真实新游戏不再在31/526/2停止，`local/key-final-probe.log`的全新存档探针为3417调用、2427帧、212文本、11选择、栈0，回到标题。旧记录通过错误CLetter映射，不能与新记录等同；这仍不是完整路线验证。
- 存读档主界面按479470/477430/476920，使用原版save/load/text图集和save_load_01.area的31个热区，100槽、每页10行、重叠页签、日期/备注、最新槽标识、左侧八角色状态簿。保存备注框使用dialog_p.akb，读档确认按4bfaa0模式5使用kisaku_DL_dialog_p.akb，位置y324，按钮热区dialog2.area加48；默认不选Yes。取消保留当前状态，确认才创建私有加载运行时，成功后替换当前实例。
- 前端专项验证实际图集、页签重叠、页首/页尾循环、备注保存、Yes/No/右键取消，以及真实检查点的异步读档。已人工查看`local/key-final-save.png`、`local/key-final-note.png`、`local/key-final-load.png`；截图来自移植程序。主机完整回归`local/key-final-regression.log`、运行时/前端ASan+UBSan及Switch构建均通过。人物详情、参数预览、进退动画、保存评论的原版IME行为与跨槽全局进度合并仍待补齐；不将主界面完成等同完整存读档恢复。
- 最终`kisaku.nro` SHA-256：`623e83861fda6a6c7415844fad383660f47b8ea0c530a5098e7382dbb9c02d3b`。交付主入口/兼容入口字节一致；未进行Switch实机复测，仍为开发预览。


### 2026-09-21：参数条、双 ELF 图块和读档白框

- 用户第一张照片：49fc40使用5351d0尺寸表，客户端宽602，高度按1..4行分别为30/59/88/112。参数私有DIB仍为602×112，49f5a0会刷新隐藏行；旧呈现把112行全部画出，造成没有底板的第二、第三条和第四行小数字外露。现将绘制及背景恢复均裁到客户端高度，逐像素覆盖全部四种行数，并检查窗口外新绘制内容不会被回滚。结束音效只播放一次；完成/跳过恢复背景后关闭窗口。
- 用户第二张照片：不能只看动画构造器的source=8。4b5661明确调用428ae0(0,9)，为41cc70返回的第二个动画管理器选择layer9；旧代码从保留开场标志的layer8取图。31/520/12经4fea10依次弹出bank、cell、临时目标（旧代码把bank与目标颠倒）；5031b0完成首帧后将目标还原0。动作1..4的bank/cell弹出顺序一起纠正，动作2经5033a0→manager+18启动state0；state1是动作3的边界停止请求，不能当作正常播放。
- 13/12经4fe8d0的同步首帧已接通；4ddcc0/405240的快速扫描对1/2/5/7操作数均读取4字节，纠正旧扫描的0/1字节步长。补充非零bank=2/cell=5、不同源/目标像素、首帧后恢复目标、连续播放与非法参数保留断言。此前引用参考游戏42a4e0的0..11范围不适用于此接口。
- 实际新游戏read_id=27743（公司门口）画面已经没有双ELF图块，主机截图local/company-front.png。未运行PC程序，截图是移植运行时输出，不能作为Switch实机复测。
- 扩大读档测试到LEV00_1首个检查点后，复现白底及ELF残留：该恢复前导只记录了差分背景EV04A和轨道选择，缺少新实例所需的前序底图/驻留AX资源。补充移植版资源上下文记录0xfffb，并以版本5槽格式将layer4底图写入同代.scene侧文件；先完成FLAG/control/preview/scene，再原子发布index。加载先验证/分配再安装，缺失或截断新底图明确失败并保持标题状态；覆盖成功后清理上一代文件。这是便携存档扩展，不宣称是PC原生SaveData格式。旧存档缺少上下文，不能保证恢复其未记录的底图。
- 白框修复后read_id=11858、参数805检查点，存档/全新标题读档的整张640×480可见RGB差异为0；local/save-later.png与local/load-later.png为诊断输出。原有前三个检查点及继续下一句测试保持通过。

### 2026-09-21：全局进度与私有窗口恢复

- 5076f0/5080a0：保存/用户退出前合并本模式原生目录（FLAG100或FLAG201）；载入时普通模式恢复1016、1022..1028、1032..1159、2000..4095但保留槽内4092，另一模式复制2000..4095；均清4007/4010/4011并复制5000..6999。普通模式word99清零、byte4090按CONFIG/Movie设置。故事变量、raw和其他word不被目录覆盖；损坏目录不覆盖、不静默忽略。25/3仍只刷已读记录，不提前提交剧情进度。
- 507b69..507d3f的参数/日记状态采用临时分配后一起替换：word500..503、541、552/553；日记人物200..295、事件300..395。31/528/20更新一天4个人物，动作21更新单个事件，按真实diary.akb和原版坐标重绘；范围和失败参数保留已测。日记天数为max(1,人物非零数/4)，不能向上取整；动作24只重置私有日记，不清VM word。
- 参数累计/剩余计数的完整MES恢复路径、后期/多层调用/全部路线、另一模式实际游玩仍未验证。本轮另一模式只验证合成状态的目录合并，不当作可玩性证明。
- 完整主机回归local/graphics-final-regression.log、save_runtime及kisaku_bootstrap的ASan/UBSan、Switch构建全部通过。新游戏探针local/graphics-final-probe.log：3417次调用、2427帧、212条文本、11次选择、栈0返回标题；不是完整路线通关。
- 本轮kisaku.nro SHA-256：acdfb2b6cb8e70b1ac24caf63716db2f456c60b5fcd8c51c6cd7762b43c432d1。同步交付，仍为开发预览，Switch硬件结果尚未验证。


## 2026-09-21：动画等待、暂停恢复与 CG 登记条件

- `31/520/5` 经 `5034f0 → 4deb80` 接入扩展 AX 的播放结束等待；`8/10` 经 `5034b0/503480 → 4dea60/4de9a0` 请求边界暂停，并按 `4de850` 等待整个管理器到达 STOPPED/4。动画 flag `0x10` 关闭时直接落实待暂停状态，避免依赖已关闭的时钟。`9/11` 经 `503260/503230 → 4de2e0/4de230` 仅恢复 state 4，保留游标、循环与延迟。普通和扩展管理器独立等待，扩展等待期间不可保存。
- `4deb80` 的跳过检查是 flag `0x20` 与输入 `0x101`；`4bfd50` 确认后者是取消/右键/Esc/Delete。因此确认键不提前结束等待；取消键仅在允许跳过时停止目标轨道，暂停屏障不接受跳过，也不会把输入交给消息窗口。
- 修正旧记录中“原生事件队列”的判断：`4057c0/4058b0` 操作的是登记轨道列表。普通/扩展管理器分别维护登记次数，重复启动保留重复登记，停止删除一个匹配项；全体停止清空，暂停完成后才注销，恢复重新登记。播放自然结束不等同注销；同步播放动作 5 不追加登记。
- `31/1011/11 → 5042d0` 根据普通管理器登记列表判断 CG：每个条件的 `(bank,cell)` 必须存在，登记总数必须精确等于条件数量；无条件图片也要求列表为空。保留同名多条媒体记录及首个匹配行为，修复原先只支持无条件 CG 的缺口。此处覆盖 `13/1..12` 和 `31/520/1..12` 的登记生命周期，不据此推断其他尚未核对的原生包装调用。
- `13/1..12` 改为先校验再消费参数；轨道越界、字符串参数、无效流与登记溢出均保留调用栈。未识别调用继续明确报错。
- 必要验证：`local/ax-manager-test.log` 包含真实 AX 指令的完成等待、边界暂停/恢复、禁用时钟、跳过权限、普通/扩展隔离；真实媒体表 `EV14A.AKB` 的 5001/5002 条件、重复登记、已停止但仍登记、无条件图片排除及调用者栈保留均通过。`local/ax-manager-probe.log` 仍为 3417 次调用、2427 帧、212 次文本、11 次选择返回标题；只是既有部分路径。存读档回归见 `local/ax-manager-save.log`，Switch 编译见 `local/ax-manager-switch.log`。
- 本轮主入口 SHA-256：`228ee162f09aea68cd86b27fc658b4bd5a3278f136272c0ee324dca8641901fb`。交付仍是开发预览；尚无此次 Switch 实机验证，不能将本轮测试当作完整路线完成。


## 2026-09-21：消息过渡 type 2/3

- `31/43/3 → 4858c0` 的剩余 type 2/3 已接通。type 2 经 `485c49 → 4b99a0` 保留旧消息精灵，单独把新内容从透明淡入；不能复用 type 1 的旧消息同时淡出。
- type 3 经 `485cd1 → 4b9850 → CSpriteLayer+84(46b1e0)`，参数为遮罩号 3、show=1、hard=0。`46ae00` 按 `%02u.bin` 加载 `03.bin`；`46a520` 取整数步长 `512/steps`，`46a650` 按原始 Alpha 与阈值差的较小值逐像素揭示。汇编 `46a847..46a890` 确认遮罩字节使用无符号扩展，Alpha 写回不改变 RGB。移植保持原始像素，绘制时应用等价 Alpha，避免修改后不能正确恢复。
- type 2/3 结束仅把当前消息矩形复制进持久表面，保留其他行，然后清空临时表面；type 1 仍按原版整面替换。透明度混合保持目标 Alpha，RGB 两项分别除以 255 取整。沿用独立 15ms 时钟及 EffectSpeed/flag0x4000 规则，零时长直接完成。遮罩不足 640×480 字节时保留参数明确报错，释放与退出均清理遮罩缓存。
- 简短验证：`local/message-reveal-test.log` 的新专项使用实际 `03.bin`，检查 type 2/3 三档速度的中间像素、旧行不消失、矩形外内容保留、结束复制、目标 Alpha、重复呈现及缺失资源时参数保留；原 type 1 回归也通过。Mac 前端与 Switch 构建更新，Switch 日志为 `local/message-reveal-switch.log`。未启动 PC 程序，不将上述像素检查等同完整剧情或实机验证。
- 本轮主入口 SHA-256：`dd47da5c70b7b987193b0601087ebdf462c776e31cd7392e0890c90762056f16`；已同步交付目录。


## 2026-09-21：姓名前缀与消息菜单按钮运动

- `482b20` 按 CP932 `816d`/`816e` 判断姓名括号，先跳过单字节前导内容，然后一次复制姓名覆盖的文字矩形，并把渐显游标移到正文。`421f80/421fd0` 的比较方向已核对。移植为长度受限扫描：纯 ASCII、非姓名括号、截断双字节不越界；姓名宽度按系统变量30、行高按31，复制限制在有效消息行内。
- `483d10` 展开四个按钮时，同步从 y=480 移至464，各自持续6/8/10/12个插值步；`483b80` 收起从464移至480，持续14/12/10/8步。不是统一瞬移，也不是依次开始。沿用原版15ms时钟、EffectSpeed和flag0x4000，端点两个按钮保持原位。
- 运动期间暂停正文渐显、自动推进和菜单选项命中；允许跳过时输入只完成运动，不推进当前对话。禁止跳过时保持运动。移动中的按钮不开放存档或前端系统菜单；结束后恢复正常点击和方向选择。
- 简短验证：`local/message-detail-test.log` 检查原生姓名字节串、单字节前导、非姓名与截断文本，逐步正文游标，四按钮中间帧像素及三档速度/flag0x4000/跳过行为；`local/message-detail-ui.log` 为前端回归。Mac前端与Switch构建更新；Switch构建日志为`local/message-detail-switch.log`。没有启动PC版，也未将这些专项结果当成全部窗口/路线或Switch实机验证。
- 本轮主入口 SHA-256：`b4cbf3d778c535c68bc99ff1d2e8bf5fb50bf7d4f3730c82b2c084390c10238d`；已同步交付目录。


## 2026-09-21：信件页进入、清页与退出

- `31/525/0 → 48aed0` 保存原始背景至私有 ec 表面，按 `48cb10 → 48b130` 对 RGB 饱和减 90，切换 24 像素明朝字体；`48a0a0` 设置文字区域 `(32,12,608,480)`、24×24 字距行距、layer1 和起始游标，清空整张文字表面。
- `31/525/3 → 48acd0` 清页先将原始背景另存至 f4，使用当前屏幕渐变到原始背景的减暗版本，结束后恢复 ec。重复清页不会累计减暗，也不会丢失退出时需要的原始背景。`31/525/2 → 48adf0` 恢复该背景，并还原普通消息区域及 16 像素字体。
- `48a960` 初始即显示一个插值步，按 EffectSpeed 使用 32/64/256 增量和独立 15ms 时钟；不套用普通消息的 flag0x4000 规则。RGB 两项分别 /255 取整，减暗不写目标 Alpha。分配失败、忙状态、非法清页及未知动作保留参数报错。
- `31/525/1 → 48afa0` 仍未实现，不能将本轮称为完整信件模式。已继续静态核对：48a640 初始化、48a360/489760/489aa0 使用 01.bin 逐行揭示，48a560 确认，48a8b0 与 48aab0/48abe0 隐藏/恢复全文，489f80 自动及语音等待；还需接通这些输入与模态状态，不能直接借用参考游戏小说模式。
- 必要验证：`local/letter-pages-test.log` 覆盖三档速度、重复清页、原始 RGB/Alpha 恢复、字体区域及参数保留，bootstrap 回归通过。Mac 前端和 Switch 构建已更新；`local/letter-pages-probe.log` 既有部分剧情探针仍为 3417 次调用、2427 帧、212 次文本、11 次选择返回标题，不覆盖信件正文或完整路线。未启动 PC 程序，也未做本轮 Switch 实机测试。
- 本轮主入口 SHA-256：`e755758ee468b5957a6ebd514b784c909a0b91ed1cee73f5a73508b668adda86`；已同步交付目录，仍为开发预览。


## 2026-09-21：信件正文模态与逐行揭示

- `31/525/1 → 4fccc0 → 48afa0` 接通正文等待和整数 0 返回，覆盖上节动作 1 的未支持记录。参数先校验再消费；未进入信件、非法游标、缺失/截断 `01.bin`、错误类型及超范围已读编号保留参数报错。原版 byte4010==1 将编号替换为27680；`4f5210` 的负数编号一律视为已读，不写已读位。正文等待清 flag0x80，返回恢复该位。
- `48a360/489760/489c90/489aa0`：保存当前屏幕，按原版 `01.bin` 逐行揭示。EffectSpeed 0/1 的阈值步长为32/64，2直接全显；行高24，第一行从私有游标开始，后续从x=0开始，到系统游标所在行结束。阈值按无符号遮罩字节计算并限制到原始字形Alpha，RGB乘积分别/255取整，保留目标Alpha。绘制不损坏layer1原始Alpha，逐帧从保存的底图合成；上一段结束游标用于下一段。
- `48a560` 一次确认即完成正文、停止语音并返回脚本；没有套用普通消息“首次只全显”的行为。`48a8b0/48aab0/48abe0` 的取消键隐藏/恢复页面已接入，切换期间不推进正文；隐藏时确认只恢复页面。`489f80/489f50` 的自动、语音结束等待与已读/强制跳过接入独立信件状态，避免普通消息的逐字逻辑及底栏按钮干扰。
- `489ea0` 的回看入口先完整显示正文，再退出信件页面，淡化完成后才打开已有回看面板，关闭后恢复信件。键盘、控制器、鼠标滚轮及触摸入口统一通过该调度。现有回看仍采用移植版64条文字/单语音记录，并非完整原生命令历史。信件存档重建、回想模式专用退出对话与完整剧情中的演出仍待核对；暂时禁止信件状态保存，不输出无法保证恢复的存档。
- 简短验证：`local/letter-body-test.log` 的新增专项检查真实01.bin、三档速度的跨行中间RGB/Alpha、连续正文游标、一次确认返回、隐藏/恢复/回看进退、自动与已读跳过、负数编号、27680替换、参数保留和缺资源失败；既有bootstrap回归通过。`local/letter-body-ui.log` 的实际前端设置/回看/存读档/确认回归通过。Mac前端与Switch构建已更新（`local/letter-body-switch.log`）；没有运行PC程序，没有此次Switch实机结果，也不把这些专项当作完整路线通关。
- 本轮主入口 SHA-256：`49c6c57575aba3d8b2727b29a58a83b2b6603d00eaae758e50ae34e761f3d04d`；交付目录已同步，仍是开发预览。


## 2026-09-21：信件页存档重建

- 静态检查原版`memo.mes`和`lev05_1.mes`：页首检查点先调用31/525/0，同页多段正文经liblary.lib函数43进入31/525/1，段落之间没有新的检查点。只跳回检查点或直接套普通消息恢复会回到页首，丢失保存位置前的文字。
- 便携控制扩展0xfffe保留版本1和目标已读编号，第三项增加mode=2表示CLetter；旧mode=1小说记录继续接受。它是移植存档扩展，不是声称还原PC存档布局。加载按原有原生恢复前导和MES检查点重建，从真实脚本逐段完整显示并确认，直到目标编号；按消息模式校验，避免普通对话意外匹配。历史快照只在目标到达后安装，不重复追加重建段落。
- 信件页正文显示完成、页面可见、没有过渡/回看/菜单请求且已读编号非负时开放保存；其他状态继续拒绝。保留原来Switch L和Mac S进入存档界面，解除信件的保存动作拦截。原版负数编号仍可显示/跳过，但没有唯一恢复标识，不允许将其写成信件检查点存档。
- 新增`tests/letter_save_test.c`并接入build-host.sh/test-host.sh：在已经初始化的日文运行时直接进入未修改的`memo.mes`，使用真实检查点、库函数及恢复前导。这是定点脚本测试，不是通关到该事件的路线证明。分别保存22208/22209/22210/22211，四次都从全新标题加载，画面RGB、历史文字/语音名、游标、正文编号相同；继续到22212再退出信件到普通对话22213，RGB差异仍为0。隐藏/过渡/负编号禁止保存；非法mode=99拒绝且保留标题实例。
- 必要验证：`local/letter-save-test.log`、`local/letter-save-normal.log`、`local/letter-save-bootstrap.log`通过，包含既有普通检查点、参数/日记、全局进度和损坏目录处理。Mac前端与Switch构建更新；Switch日志`local/letter-save-switch.log`。信件后期路径、多层脚本调用和实机仍未验证，回想专用退出对话仍待补齐。
- 本轮主入口 SHA-256：`89290894cff148d24efc77688965f54f325424c44445619c167fa7f3aadfcb3a`；已同步交付目录，仍为开发预览。


## 2026-09-21：信件回想退出确认

- `48a8b0` 在全文可见时先全显并隐藏信件；隐藏时再次取消，若byte4010非零则调用`4b3ba0`。新增此独立等待状态，普通信件仍直接隐藏/恢复。确认框期间隔离确认、取消和其他消息操作，避免输入穿透到正文。
- `4b3ba0 → 4bfaa0(7,15) → 4bf530`使用原版`kisaku_DL_dialog_p.akb`：428×88框位于(106,196)，提示源(0,288,276,24)贴至框内(76,20)，是「シーンタイトルに戻りますか？」；Yes/No图块与dialog.area热区沿用原版，初始无选中项。键鼠、控制器和触摸统一处理；No、右键取消、触摸长按均恢复信件，保持正文调用位置。
- Yes按`4b3ba0 → 4e2b50/4e2820`根据bank1[61]选择`scene.mes`或`hage_scmode.mes`，清除旧操作数/调用栈并从脚本入口开始，不向旧信件调用者压入普通返回值。资源先加载校验，失败保留原对话、脚本位置和参数栈，面板显示失败；不将回到场景选择等同退出游戏或回标题。
- 必要验证：`local/letter-exit-ui.log`以实际memo.mes正文和byte4010回想标记检查两种返回目标、原版提示像素、No/右键/触摸长按、输入隔离和缺失返回脚本时的保留；既有设置/回看/存读档前端回归通过。人工检查`local/letter-exit.png`，这是移植程序截图。`local/letter-exit-bootstrap.log`与`local/letter-exit-save.log`通过，Mac/Switch编译已更新，Switch日志`local/letter-exit-switch.log`。
- **后续菜单仍未完成**：额外继续运行返回脚本，普通模式在`scene.mes+0x10c`的31/812/1明确报错、保留2项参数；另一模式在`hage_scmode.mes+0x3c9`的19图层操作报unallocated layer。仅验证了退出对话和脚本切换，不能宣称可以正常返回完整回想菜单。31/812分发为4f9bb0，静态看涉及byte3570起24字节与sys70字符串往返，仍需核对41cfa0/41cde0和汇编后实现；另一模式要继续追查其初始化图层。旧scene_replay.inc仍含参考游戏8192布局，不能当作《鬼作》回想验证。
- 本轮主入口 SHA-256：`74c95304bf93d64c6d00cf5a4d1cb1db3e14a394e5ee4bd038576317c182ec2f`；已同步交付目录，仍为开发预览，未做本轮Switch实机验证。

### 2026-09-21：回想返回的场景名与 layer0 修正

- `31/812 → 4f9bb0` 已按原版字节布局接通。动作0把系统字符串70以 `strcpy_s` 语义写入 byte 3570..3593 的24字节区；动作1把该区内容恢复到自有字符串变量。CP932长度、终止字节、容量不足和非法参数均在消费前检查，错误时保留调用者栈。
- `4f60e0 → 4f6040 → 502c90` 的图像目的层允许 layer0。原版 `hage_scmode.mes` 会把 `bg04.akb` 直接装到显示层0；同步和异步路径现在都写入场景表面，不再错误报“unallocated layer”。
- 实际信件回想退出验证：普通模式 `scene.mes+0x393`、另一模式 `hage_scmode.mes+0x402` 均越过上述两处错误，随后在原生 `31/320 (4fae70)` 场景选择接口停止。该接口对应 CScMode/CHageScMode 模态和专用资源，当前仍保留参数明确报错，没有把现有前端导航菜单冒充原生实现。
- 简短验证：`local/scene-context-host.log` 的场景名/图层专项、信件返回与前端回归通过；`local/scene-context-switch.log` 为空表示 Switch 编译通过；`git diff --check` 通过。没有启动 PC 程序，也没有 Switch 实机复测。
- 本轮主入口 SHA-256：`2e87bab48b31e2c3b85e0f861b44cc875e226c65eb58aeddfc728b2775fac5ae`；已同步交付目录，仍为开发预览。

### 2026-09-21：31/320 回想场景选择与回放接通

- `31/320 → 4fae70` 已从未映射边界接入模态状态。该调用没有脚本参数；原生选择器返回值留在 VM 栈，移植在 `scene_modal` 中暂停脚本，由前端完成输入后再返回选择值或 `-1`。
- 场景目录沿用 `4d5e80` 的600槽表，新增目录查询接口。选择器显示全部有效槽位，已完成槽读取 `SceneData` 的完成字节并标记“解锁/未解锁”；未解锁项不能启动回放。普通/另一模式都使用原版 `kisaku_DL_seanemode_p.akb` 与 `kisaku_DL_seanemode_p2.akb` 作为选择层，CP932场景号/分段文字叠加在相同640×480画面。
- 确认已解锁项把系统变量18和返回值设为对应场景号，并把精确的SceneData槽交给已有 `4755f0` 回放准备器；回放结束后合并进度并回到原脚本。取消返回 `-1`，不破坏原操作数栈。未解锁确认只显示状态，不推进脚本。
- `tests/message_panel_test.c` 现在覆盖两种回想脚本、mode7退出对话、31/320真实停点、600槽锁定显示、原版AKB层解码、取消返回值，以及已解锁槽选择结果。`local/scene-mode-host-final.log` 的完整主机回归、`local/scene-mode-switch.log`、`git diff --check` 通过；仍未启动PC程序，也没有Switch实机复测。
- 本轮主入口 SHA-256：`8e434e3036a27810dea644c4e266af955387cc13630d11837297262895b6d1e1`；交付目录已同步，仍为开发预览。新增 Mac/触摸命中区域与控制器共用同一选择路径。完整路线和后期原生接口不因回想选择器接通而视为全部完成。

### 2026-09-21：31/320 场景选择器按两套 AREA 坐标恢复

- 重新核对 `4fae70 -> 4a7d40/472700`：普通 `CScMode` 使用 `scmode.area`，秃作 `CHageScMode` 使用 `hage_scmode.area`；两者均为 3×3 场景格，每页 9 个槽位，退出区域在底部，不能用旧预览的十行列表。
- `tools/navigation_panel.inc` 现在按 `global[1][61]` 区分两套坐标，方向键按 3×3 网格移动，鼠标/触摸点击命中原版格子范围，页移动按 9 槽步进；画面底部标题区分普通回想与秃作回想。锁定判断仍读取 `SceneData` 完成字节，播放继续使用同一槽位的脚本名与检查点。
- 主机完整回归、场景选择专项、Switch 构建和 `git diff --check` 通过；仍未进行 Switch 实机测试，完整路线及后期脚本不能据此视为完成。

### 2026-09-21：31/320 AREA 页签与退出命中修正

- `scmode.area` 的普通模式页签为 `0x100..0x10a`，`hage_scmode.area` 的秃作模式页签为 `0x100..0x103`；指针输入现在按各自原版坐标切换九槽页。
- 两套 AREA 的 `(528,448)-(640,480)` 都是退出热区。修正旧前端把右下角误判成“下一页”的问题，控制器、鼠标和触摸仍共用同一取消路径，取消会向 VM 返回 `-1` 并恢复脚本。
- 主机完整回归、场景专项、Switch 构建、交付同步及 `git diff --check` 通过；本轮 `kisaku.nro` SHA-256 为 `8e434e3036a27810dea644c4e266af955387cc13630d11837297262895b6d1e1`。仍未进行 Switch 实机测试。

### 2026-09-21：完整路线第一轮矩阵探针

- `tools/bootstrap_probe.c` 增加 `KISAKU_CHOICE_SEQUENCE`（逗号分隔的选择索引），可在不改脚本、不启动 PC 程序的前提下固定真实日文脚本的选择顺序。
- 对开场选择索引 0..7 分别运行 30000 帧，并用 `first` 处理后续可用选项：8 条路径均未触发新的未支持接口或崩溃，均回到标题；调用数为 3282..3444、文本数为 187..217。该结果只是第一轮路径烟雾测试，不能代替每条路线的结局、后期脚本和存档往返验证。
- `test-host.sh`、资源审计、`git diff --check` 通过。下一步继续用选择序列覆盖后续分支，并记录第一处不同的原生调用。

### 2026-09-21：完整路线前三层选择矩阵

- 固定日文脚本前三次选择（0..7）共 512 组，均在 30000 帧预算内正常返回，未出现 `unsupported`、运行时错误或崩溃；调用数范围 3192..3468，文本数 183..221。
- 结果说明移植 VM 和已接通接口能承受前三层选择组合，但“回到标题”仍只是脚本探针结果；每条路线的结局内容、后期脚本、回想解锁与存档往返仍需继续覆盖。探针输出保存在 `local/route-matrix-3/`。

### 2026-09-21：31/526/0 位置标签创建路径

- `31/526/0 → 4fd790 → 47b1c0/47b220/47b020` 已接通。调用按 VM 逆序参数读取源矩形 `(x,y,width,height)` 与右侧字节数，创建两个同尺寸私有表面和独立 backing；第一表面从 layer7 复制原生绿色键控区域，第二表面保留独立透明表面，再按 `x=608-right_bytes*8,y=16` 安装。非法源矩形、目标范围、尺寸和分配失败均在消费前保留参数并报错。
- 原有 `31/526/2` 位置标签继续使用固定 336×32 表面；恢复/呈现保留原版右边界裁剪，避免名称较长时把 336 像素表面错误当成必须完全落屏。释放动作清理两个私有表面、backing 和状态。
- `local/location-create-regression.log` 覆盖 40 个 CP932 地点名、右侧裁剪、键控像素、重复呈现不累积、无关 UI 写入保留、创建/释放和错误参数栈；主机完整回归、Switch 构建、交付同步及 `git diff --check` 通过。本轮主入口 SHA-256：`5f9928c3f9a955ed6e9f9c2e17895845494d66a513d89a5e012128a023d55ce9`。
- 该接口的后续动作仍按未核对边界保留明确报错；路线矩阵和此专项都不能代替 Switch 实机或完整结局验证。

### 2026-09-21：四层路线矩阵与 name.mes 边界

- 固定前四次选择索引 `0..7` 共运行 4096 条日文脚本序列。除一条较深路径外均在预算内返回标题；唯一首个新边界为 `5,2,3,3` 进入 `lev00_1.mes` 的 `31/527`，没有出现崩溃、ASan、资源解码或音频错误。该序列在接通前置初始化后进一步到达 `save.mes`，说明路线探针确实跨过了 name.mes 与读档前导。
- `29/0` 按 `CFuncBackLog::virtual_0 (4fe5f0→4fe5b0→4fe580)` 接通空历史容器初始化；`31/810` 按 `4fd3f0` 打开现有 CP932 名称编辑器状态，`31/811` 按 `4fd6c0` 清理名称辅助对象，名称字节仍保留在 VM 的 1950 区。Mac/探针自动输入只用于无交互烟雾覆盖，实际前端仍提供名称编辑。
- `31/527 (4f9dc0)` 的动作 0/1 还依赖原生输入门和等待状态；尝试用简单输入标志替代会导致选择循环和操作数栈增长，已撤回并继续保留参数报错，避免伪造成功。复核日志为 `local/route-check-5-2-3-3i.log`。
- 进一步静态核对 `4f9dc0`：它先经 `4b70e0→46f290` 从 `CFuncExec+8` 的 IFlag 队列取事件，再由 `IFlag+8` 的事件码选择 0/1/2 分支；分支 0/1 只对 `CFuncExec+4` 关联状态字 OR `0x10/0x08`，分支 2 从上下文数组第60项构造 IFlag 后回送队列。事件码不是 VM 栈上的 `527` 或前置零值，不能按普通 syscall 参数映射。仅消费一个栈值的实验在 `local/route-527-consume-only.log` 中出现 361 项栈增长和 367 次选择循环，已撤回；当前仍保留原始参数并明确报错，等待把输入队列、状态字和回送路径接入统一输入模型。
- `test-host.sh`、资源审计、Switch 构建、交付同步及 `git diff --check` 通过。本轮主入口 SHA-256：`7bb6eb4ff0b277f5b6b6cc2df2cb42d9cf7db1863e3f2b5b431fec3b6b4884b7`。矩阵与探针均不是完整结局或 Switch 实机验证。

### 2026-09-21：31/527 输入轮询边界接通

- `31/527 → 4f9dc0` 已按 CFuncExec 的静态调用链接入轮询语义：VM 只消费事件选择器，调用者前置的两个值保留；便携输入的确认/取消边沿分别映射到原生状态字 `0x10/0x08`，每次轮询后清除边沿。没有伪造原生分支 2 的 IFlag 回送事件。
- `tests/kisaku_bootstrap_test.c` 增加空轮询、双边沿、参数栈与状态字断言。这样 `save.mes` 的 31/527 不再被误判为普通带参接口；后续日誌选择仍需实际事件序列和脚本回归覆盖，不能据此宣称全路线完成。
- 本轮主机回归、资源审计、Switch 构建、交付同步及 `git diff --check` 均通过；主入口 `kisaku.nro` SHA-256 为 `1f4aeb418982576506bbed45694c697cf56d4f92893bd053e80af551dde9169b`。路线探针已进入 `save.mes @0x375` 的 31/527，当前自动选项策略会在后续日志选择重复同一项而循环；这属于探针策略限制，不是 31/527 崩溃，仍不能替代 Switch 实机和完整路线验证。
- 追加 `KISAKU_CHOICE_SEQUENCE=5,2,3,3 --choice cycle --frames 30000` 后，31/527 后的 `elf-chang.ax` 双轨动画和开场音效序列均能收尾，探针回到标题（7916 次调用、10053 帧、435 条文本、栈深 0）。首层 0..7 八条独立新游戏探针也全部回到标题，未出现新的 unsupported、运行时错误或崩溃；结果仍属于脚本烟雾验证，不等于完整结局和 Switch 实机验证。
- 进一步复核三层选择矩阵 512/512 组（每组 10000 帧预算）全部回到标题，日志未发现 `unsupported`、运行时错误、ASan/UBSan 或崩溃；原始日志保存在 `local/route-matrix-3-current/`。这是已接通接口的组合回归，仍不覆盖全部后期路线、小游戏和 Switch 实机。

### 2026-09-22：31/527 路线回归与交付复核

- 重新执行 `test-host.sh`、资源审计、`build-switch.sh`、`package_sd.py` 和 `git diff --check`，全部通过；主机回归日志为 `local/continue-regression.log`，Switch 构建日志为 `local/continue-switch-build.log`。
- `build-switch/kisaku.nro` 与 `交付/SD卡根目录/switch/kisaku/kisaku.nro` 内容一致，SHA-256 均为 `1f4aeb418982576506bbed45694c697cf56d4f92893bd053e80af551dde9169b`。仍按开发预览交付，未把脚本探针结果当作 Switch 实机或完整路线通关证明。
- 追加第四层日文选择矩阵 4096/4096 组：前 4056 组使用 4000 帧预算，40 组因参数动画预算不足再以 30000 帧复跑，最终全部回到标题；未发现 `31/612/0/1`、`unsupported`、运行时错误、ASan/UBSan 或崩溃。该结果确认已接通的剧情接口可承受四层选择组合，但不等于所有结局、小游戏和 Switch 实机验证已完成。

### 2026-09-22：交付前快速验证

- `./test-host.sh`、`tools/audit.py`、`./build-switch.sh`、`tools/package_sd.py` 和 `git diff --check` 均通过；主机回归日志为 `local/latest-simple-validation.log`，资源审计为 `local/latest-resource-audit.log`，Switch 构建为 `local/latest-switch-build.log`。
- `build-switch/kisaku.nro` 与 `交付/SD卡根目录/switch/kisaku/kisaku.nro` 内容一致，SHA-256 均为 `1f4aeb418982576506bbed45694c697cf56d4f92893bd053e80af551dde9169b`。
- 该验证仍是静态移植的主机/交叉编译检查；没有启动 PC 程序，也没有把 `31/612` 保龄球动作 0/1、全部后期路线或 Switch 实机结果冒充已完成。

### 2026-09-22：31/612 参数 ABI 边界复核

- `4fb5a0` 的事件 0 会先从 CFuncExec 队列取五个数再进入 `4d5390` 构造器；事件 1 不带构造参数，运行 `4d46b0` 后把完成值压回调用栈；事件 2 才是 `4cfd20` 释放路径。移植端现在分别保留事件 0/1 的完整参数并给出明确错误，事件 2 继续执行已验证的释放。
- 新增主机断言覆盖事件 0 的五值栈布局、事件 1 的保留语义、错误时 `sp` 不变化和事件 2 的 39 槽释放。尚未把未知的保龄球对象更新/绘制循环伪装成成功调用。

### 2026-09-22：开场音效资源名纠正

- 反汇编路径中的旧参考运行时曾使用 `logo01.wav`/`logo02.wav`，但《鬼作》日文 `effect.arc` 实际资源名为 `logo.wav` 与 `potapota.wav`。31/24 的开场阶段已改为读取这两个真实资源，避免启动时静音或资源缺失。
- `local/logo-resource-fix.log` 的主机回归、资源审计和音频 PCM 非零检查通过；Switch 交叉编译与 SD 卡目录同步在本轮完成。该修正不改变未知原生接口的报错边界。
- 本轮 `build-switch/kisaku.nro` 与交付目录副本 SHA-256 均为 `da9929681b7a47b5ad12cda222aedd725082f278239c4099e101aea60bd86d7d`。

### 2026-09-22：设置音量滑块试听

- 静态核对 `4c2180/4c23c0/4c25e0/4c8da0`，补上音乐、普通音效、语音、场景音效四组滑块试听。原版无播放对象时分别使用 `bgm01.wav`、`Akikaze.wav`、`z09588.ogg`、`Piss.wav`；普通音效的音量调整排除独立的 channel3。汇编复核保存在 `local/config-audio-disassembly.txt`。
- 新增 `tools/config_audio.h`，音量在输出时按原版 0..104/dB 公式计算，原始 PCM 不改写；有可用的当前音轨时借用只读 PCM 和独立游标，没有时缓存上述样本。鼠标/触摸松手结束临时样本，现有音轨试听保持到切页/退出；键盘/手柄调整采用末次操作后250ms释放临时样本。没有消费剧情语音工作线程的待取结果，也不写入设置草稿或剧情游标。
- 设置与回看共用保存 SDL 排队音频的机制，退出、切页和取消会清理试听并恢复原有队列。设置关闭后释放四个样本缓存；失焦或失去拖动状态也释放临时试听。当前实现保持移植程序在设置期间暂停剧情的策略，以独立游标试听所选类别；并非原版所有类别继续同时播放的完整音频时钟还原。

### 2026-09-22：存档参数预览与展开窗口

- 按 `00474500 -> 004747a0` 从所选日文存档的 words 500..503、552/553、556/557 和行数541重建 `CParamaterWnd`。缩略图按原版 268 像素宽和四种裁剪高度写入角色簿下方；点击参数区域显示 602×112 原始窗口，背景先以 63/255 变暗，再按 `004777e0` 的 24/12/6/0 步淡入。坏标记、负值、截断 words 或缺少素材会拒绝预览并保留当前游戏状态。
- 展开窗口期间隔离存档页签、槽位和保存动作；确认/取消在淡入完成后关闭。Shift/Ctrl 现在按原版 102/103 立即完成淡入，触摸点击、鼠标右键和 B 键共用同一关闭路径。预览使用临时 bootstrap，不修改运行中的参数表面、VM 栈、存档数据或设置草稿。
- `test-host.sh`、ASan、资源审计、Switch 交叉编译、`package_sd.py 鬼作` 和 `git diff --check` 通过。专项日志为 `local/save-param-asan.log`、`local/param-skip-test.log`；最新 `build-switch/kisaku.nro` 与交付目录副本 SHA-256 为 `943f073ce792970383d234be97283c0efe2464835c78575167f009d0cc3fe291`。字符簿的角色详情页（AREA group5）和保龄球创建/运行仍保留明确未支持边界；未启动 PC 程序，也未进行 Switch 实机验证。

### 2026-09-22：存档角色簿详情入口

- `00479c10 -> 004b2110` 的 group5 入口已接通。点击角色簿的原版 `0x500..0x507` 热区时，先检查当前存档的解锁字节；未解锁项只显示日文提示，不消费槽位或 VM 状态。已解锁项进入独立详情模态，加载真实 `statfr.akb`，绘制角色标题和返回提示，确认、取消、触摸和鼠标输入都不会穿透到存档页。
- 这是详情入口和真实底图的移植，角色状态条、语音记录及原版各角色专用 `stat*` 图集的逐项组合仍需继续核对，不能把入口专项当作完整角色簿复原。
- `test-host.sh`、Switch 构建、资源审计和 `git diff --check` 通过；专项日志为 `local/character-detail-final-test.log`。角色详情真实底图和八套 `stat*.akb` 状态条均已解码；本轮 `build-switch/kisaku.nro` 与交付副本 SHA-256 为 `0d9c93415e4dadb6aeafa71d39753ec04d97ee6cf7cdc403e3c01d295c95c59b`。
- 前端专项验证四个真实样本、满音量逐样本相等、52档逐样本衰减、静音、channel3隔离、循环、不改变剧情游标/持久设置、缺失资源、待取语音、触摸松手、手柄试听和取消时逐字节还原SDL队列。`local/config-audition-ui.log` 与 `local/config-audition-asan.log` 通过；ASan/UBSan未报错，macOS不支持LeakSanitizer，未声称完成泄漏检测。
- 设置进退与切页动画、完整原生命令回看、保龄球创建/运行、全部后期路线和Switch实机仍需继续处理；本轮不把试听补齐等同完整移植结束。
- 最终主机构建、完整回归 `local/config-audition-regression.log`、Switch编译 `local/config-audition-switch.log`、SD卡同步及 `git diff --check` 通过。构建和交付主入口、兼容名的SHA-256一致：`075e26e1ad79e6dbfc508aa88a35dc69d755eb756f4d493d0cfc0a6e2f018fa0`。

### 2026-09-22：设置页面进退和切页演出

- 按 `4c8800/4c82f0/4c69e0/4c67c0` 接入进入、退出、旧页右移600像素、新页从右侧600像素移入；进入时预览精灵单独从640像素移入。页签从y=-20移入、底栏从y=480移入，标题/遮罩淡入；侧栏按`467010/466ed0`从x=-112进退。原版退出保持背景遮罩直到收尾，未擅自加入整面交叉淡化。退出演出完成后才把模态控制权交给剧情或侧栏目标面板。
- `46bbe0/46b5d0/46c010/46bca0` 决定24步基准、三档12/6/0步、flag0x4000保留24步，以及15ms独立时钟；坐标按原版整数插值。`4bfd50`核对102/103是Shift/Ctrl，只有这两个键可快速收尾，普通确认/取消不会在运动中激活控件。连续切页保留旧页退出与新页进入两个阶段。
- `4c6640/4c6480` 的角色语音子页淡入淡出已接入；取消先恢复33项草稿快照再退出子页。动画期间关闭控件命中和拖动，触摸归属键包含动画状态，防止按下时页面在移动、松手时却激活新页面。试听仍在切页/退出时停止并恢复原有音频队列。
- `local/config-motion-test.log`覆盖三档及flag0x4000、进入初帧/中间帧、预览孔关闭、双阶段切页、往返后的逐像素一致、子页淡化、取消退出和跨动画触摸隔离；设置、试听、回看、存读档、信件回想前端回归同时通过。`local/config-motion-asan.log`前端ASan/UBSan通过。静态汇编复核见`local/config-motion-disassembly.txt`；人工查看`local/config-motion-enter.png`和`local/config-motion-voice.png`，为移植程序在纯色测试底图上的截图，并非PC版或实机截图。
- 本节覆盖此前设置进退/切页动画的待办。完整原生命令回看、保龄球创建/运行、全部后期路线及Switch实机验证仍未完成，不能把本轮界面演出专项当作完整移植证明。

### 2026-09-22：演出基础层节奏夹具（转场节奏专项）

- 新增 `tests/transition_cadence_test.c` 并接入 `build-host.sh` / `test-host.sh`：它驱动真实的 `bootstrap_frame` 帧循环，逐帧测量演出基础层的三个原语，并把帧数写进回归日志——`31/21/0` 淡入、`31/21/1` 淡出、`31/17` 黑带闪烁、`31/30/0` 画布过渡。此前这些原语只有其他专项里的像素断言间接覆盖，"帧数和时长"从未以数字形式固定下来。本专项的完整数字、纠错过程与 OPEN 项见 `reports/移植记录-演出节奏专项.md`。
- **撤回一处无依据的运行时修改**：本专项曾把 `blink_frame` 重设 `blink_wait` 时改为同时 `blink_clock=0`，理由是"原实现的全黑停留只有 11 帧（183ms）"。该理由经复核**不成立**：不带该修改时实测全黑停留同样是 **12 帧（200ms）**；被改变的是关闭斜坡——每步 100 单位的余数累积，使最后两个关闭步落在同一帧，整段因此是 30 帧而不是 31 帧。该修改与其依据一并撤回，`runtime/distort.inc` 的时序行为保持原样。
- **引用地址纠错**：原注释把该时序归到 `42c890`。静态核对 `鬼作/AI6WIN.exe`（r2 反汇编 + `local/decompiled/` 导出）：`42c890` 不是函数起点，落在 `CRendererInputPin.virtual_16`（`0x42c840`，DirectShow pin/临界区代码）内部，与演出无关——它是**参考工程的地址**，按 `AGENTS.md` 只适用于参考游戏。`runtime/distort.inc` 的 `42bd20`、`42c180` 同属这一类，均已就地标注为"未核对"。新增 `tools/check_address_citations.py`：`runtime/` 共引用 138 个地址，97 个是导出中的函数起点，41 个不是（清单见 `reports/address-citations.json`）。该工具是启发式筛查，只提示候选，不等同于逐条定性——`0x507b69` 一类可能是栈值或非代码常量。
- `31/30/0` 的步数会先被 Display/EffectSpeed 与 flag `0x4000` 缩放，因此夹具测两次：按当前设置一次，再中和这两项缩放一次，后者固定为 49 帧对应 48 个请求步；淡入/淡出分别固定为 64/64 与 32/32 帧。夹具按淡出→闪烁→画布过渡的顺序执行，因为启动脚本会把淡入覆盖层留在可见状态，而闪烁与画布过渡的原生入口都要求覆盖层已经隐藏。夹具还断言隐藏状态下的 `31/21/1` 不会重新武装动画。
- 验证：`local/ds-test.log` 的完整主机回归 51 项全部通过（含节奏夹具与既有 bootstrap/save/letter/message-panel 全部专项），`build-host.sh` 在 `-Werror` 下通过。没有启动 PC 程序：帧数来自移植运行时自身的帧循环，是把**移植实现的现状**固定下来，**不是**与 PC 参考帧的逐帧比对；Switch 实机与 PC 对照仍由用户验收。
- 待办（OPEN）：`31/17` 的关闭斜坡应当是每步一帧（31 帧）还是把最后两步并到一帧（30 帧），目前**缺《鬼作》原生汇编依据**，不得按参考工程地址下结论——夹具暂时把 30 帧的现状固定为特征化断言，`reports/porting.md` 记 OPEN。`31/30` 的过渡曲线与 PC 参考帧仍需逐帧核对；12 帧全黑停留是在移植运行时的 60Hz 帧驱动假设下成立的，若后续引入真实毫秒时钟需一并复核。未改动的 `31/x` 边界（`31/520` 原生事件队列、`31/612/0/1`）继续保留参数并明确报错；`31/528/9` 的窗口/队列/音效生命周期已按 2026-09-21「31/528/9 参数演出接通」一节接通，**不再**列为未改动边界（此前本节误抄了过期缺口）。

### 2026-09-22：包 1a —— 回想回放与 New Game 初期化的堆容量检查（8192 → 9192）

- `runtime/scene_replay.inc`：`bootstrap_scene_replay_begin` 原先要求源 VM `byte_count == 8192`。8192 只是 `kvm_create` 的初始值；《鬼作》运行时在 `14/0` 之后实际声明 9192 字节（word 600 / global 15000 / bank1 100），因此该检查对真实运行时**恒为假**，回想回放每次都直接返回失败、根本起不来。改为接受 9192，并在注释中写明 8192 的来源与不再作为判定依据的理由。
- `runtime/history_reset.inc`：`bootstrap_history_reset` 同一个 8192 判定让 New Game 的"初期化"四个勾选框永久报 `history reset state invalid`。同时该函数的暂存缓冲 `uint8_t bytes[8192]` 与逐字节清零长度一并改为 `sizeof(bytes)`（9192），使暂存区与原生 byte 区等长，不再截断。
- 验证：`build-host.sh` 在 `-Werror` 下通过；`test-host.sh` 完整主机回归 51 项全部通过（`local/ds-test.log`），其中 `kisaku-bootstrap-test`、`save-runtime-test`、`letter-save-test` 覆盖本次两处改动的存档合并与回放路径；资源审计 0 失败、AKB 2191/2191 解码。日志写在 `local/ds-test.log`（本次重跑）与 `local/deepseek-cadence-regression.log`（当轮）。
- 边界：本轮只修容量判定，**没有**把回想回放与初期化的下游脚本路径当作已验证的完整流程；`31/1012`（681 个 MES、2479 次调用）等未接通接口继续保留参数并明确报错。未启动 PC 程序，Switch 实机未验证。
- 本包的独立交付记录（改动范围、复现命令、回归日志、边界）见 `reports/移植记录-包01a.md`。

### 2026-09-22：CBowling 资源门槛

- 新增 `tests/bowling_asset_test.c`，从真实 `layer.arc`/`data.arc` 读取 `bow_ball.akb`、`bow_bg.akb`、`bow_pin.akb`、`bow_pt.akb`、`bow_pt02.akb`、`bow_sr.akb`、`tutbow.akb`，逐项执行 AKB 解码；同时验证 `bow.ax` 可装载、轨道 0 的边界计数有限，并从 `effect.arc` 核对构造器使用的 8 个音效条目。
- 该专项只证明素材和轨道输入可靠，不把未知的 CBowling 窗口、投球输入、计分和完成返回伪装为动作 0/1 已实现；这两个动作仍保留参数并明确报错。

### 2026-09-22：原生 31/1012 修正、31/41 与脚本退出确认

- 撤回 `950172c` 对 `31/1012` 的参考游戏画廊接线。原版 `4f98b0 -> 5040a0` 在选择号之外只弹出一个字符串，查询 `503db0` 初始化的 189 条媒体关联表；命中后写对应 byte 索引及 4001/4004，未命中不写入、不返回栈值。使用已有 EXE 静态提取表，保留重复键的首次匹配；写入前完整校验两个索引，错误保留全部参数。`gallery.c/.h` 恢复原 API，不再依赖 `.rmt` 或 `bmptbl.dat` 解锁本接口。
- `31/41` 按 `4f9680 -> 48d860 -> 48b810` 实现 640×480 的 layer0→2 快照（含 Alpha）及 layer2→3 色调转换：B 为三个通道整数平均，G/R 为平均加16并饱和，目标 Alpha 不变。三张表面先校验再写入，调用只消费选择号。夹具逐像素检查 RGB/Alpha、带 padding 的表面和失败原子性。
- `31/3` 按 `4fbdb0 -> 4b4060` 接入已有 CDialog mode0。脚本调用期间冻结 VM/帧与剧情输入，No/取消不压入返回值，Yes 先刷新现有进度再请求退出；刷新失败保留模态。采用移植端现有 SDL 对话框及音频策略，不声称逐条复刻 Win32 菜单或全部音轨时钟。
- 修复角色详情状态图仅在首次加载时绘制的问题，缓存已加载后仍参与每次合成；专项强制刷新两次并比较整个输出。角色状态的动态选择与语音组合仍不等于已完成。
- `./build-host.sh` 与 `./test-host.sh 鬼作` 通过，日志在 `local/native-closeout/`；覆盖新增原生 ABI、模态、保存失败和重绘检查，以及既有存档/信件/容量/前端回归，AKB 2191/2191 解码，资源审计无失败。没有执行 PC EXE。
- 仍未完成：保龄球动作0/1、完整原生命令回看、角色簿动态状态与语音、其余未核对接口。全路线、后期真实场景回放及 Switch 实机未验证；本轮不能标记移植工程完成。
- `./build-switch.sh`、`python3 tools/package_sd.py 鬼作`、`git diff --check` 通过。主入口及兼容名、交付目录副本 SHA-256 均为 `086e0365366b13df6e3394331a28896653c29f60d60ceab4a7109af80038712f`。交叉编译不等于实机回归。
