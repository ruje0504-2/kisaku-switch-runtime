# AI6WIN 同引擎移植：高清文字、呈现与多核心优化笔记

记录日期：2026-09-23。多核优化基线：`9f238f3`；之前的显示正确性修复：`bdc9a53`。同日补充GPU双三次放大，见第13节；下文正常GPU路径已同步更新。

这份笔记记录《鬼作》实际落地的方案，供同引擎另一游戏复用。**可以复用呈现结构、缓存与验证方法；游戏接口、图层编号、坐标、时序和字体作用域需要重新核对。** 本轮有主机回归、实际 SDL/GLES 像素检查和线程竞态检查，尚无 Switch 实机性能结论。原始素材与 PC 可执行文件不属于可复制的代码模块。

## 1. 保留原始逻辑分辨率，单独提高输出质量

原游戏仍按 640×480 处理 VM、图层、颜色键、Alpha、脚本截图、命中坐标、存档重建和动画。输出层将画面放到 1280×720 逻辑窗口中的 `(160,0,960,720)`，保持 4:3。

高清文字是在 FreeType 中按目标字号重新生成字形，例如 16×16 的逻辑字格对应 24×24 的输出字格；不是将原先16像素的字图拉大。背景仍是原始素材的重建放大，没有获得素材本来不存在的细节。

当前管线：

```text
主线程：输入 → VM/原生演出 → 原始640×480画面
                            │
                            ├─提交当前帧准备任务─────────┐
主线程：音频混音、SDL清屏    │                           │
                            │ 第三核：清除低清字的底图  │
                            │         +960×720文字层    │
主线程：等待当前任务完成 ←──┴───────────────────────────┘
        │
        ├─底图上传/复用 → GLES限幅双三次采样及可选锐化
        ├─高清文字变化行上传/复用 → Alpha叠加
        └─模态面板、其他覆盖层、光标 → Present → 60Hz节拍
```

文字在底图锐化之后叠加，避免同一字形又被底图锐化加工。原版图片文字仍是图片；该方案不会自动将图集上的字变成高清矢量字。

## 2. 可以从哪些文件开始复用

| 文件/入口 | 作用 | 迁移要求 |
|---|---|---|
| `runtime/font.c`、`font.h` | 字形缓存、HOS字体、描边 | 保留字体实例隔离，核对字符编码和字格 |
| `runtime/bootstrap.c`：`present_text_reset`、`present_reveal`、`present_choice_glyph`、`bootstrap_present_layers` | 高清伴随文字、揭示和底图重建 | 游戏耦合最强，不能整段盲搬 |
| `tools/present_worker.inc` | 当前帧准备线程 | 替换bootstrap回调并保留独占生命周期 |
| `tools/present_gles.inc` | 自有纹理、着色器、GL状态保存恢复 | 目前尺寸硬编码为640×480，窗口假定1280×720 |
| `tools/texture_cache.inc` | 行比较、变化区域上传、纹理复用 | 固定同一renderer及blend策略；重建renderer时清缓存 |
| `runtime/present_filter.c`、`.h` | CPU放大/锐化后备与nearest参考 | 注意与GPU采样差异，见第5节 |
| `tools/runtime_viewer.c` | 提交/等待任务、各层绘制顺序、帧率 | 必须审查所有退出和换runtime路径 |
| `runtime/voice_worker.c`、`image_worker.c` | 分核解码 | 各自持有归档，不与主线程共享文件游标 |
| `tests/present_gles_test.c`、`present_gles_render_test.c`、`present_sdl_test.c` | 状态夹具、真实GPU执行及SDL菜单集成 | 三层检查都保留，仅软件renderer测试不够 |
| `tests/kisaku_bootstrap_test.c` 的 `--hires` | 逐字、选项、并发、原图不变 | 换成目标游戏实际开场/选项检查点 |
| `tests/present_benchmark.c` | 固定剧情状态的像素哈希和CPU微基准 | 比较同一状态、字体、设置和素材 |

`.inc` 文件依赖包含它们的翻译单元提供 `KImage`、SDL、bootstrap或libnx类型，不是拿出一个文件即可独立编译的库。构建依赖包括 FreeType、SDL2接口、GLES2；主机实际GLES检查另用Mesa EGL，Switch线程使用libnx。

## 3. 高清文字如何与原版图层同步

保留原始低清文字，另存高清伴随缓冲：

| 缓冲 | 《鬼作》尺寸 | 用途 |
|---|---:|---|
| `present_source_text` | 960×126 | 对应原始640×84文字源层 |
| `present_message_text` | 864×81 | 对应正文576×54的已揭示部分 |
| `present_choice_text` | 960×720 | 当前选项文字 |
| `present_shadow` | 640×84 | 核对原始文字源是否被其他指令改写 |
| `present_clean` | 640×480 | 供放大的无低清文字底图 |
| `present_reference` | 640×480 | 按原版方式含低清文字的比较参考 |
| `present_overlay` | 960×720 | 最终高清透明字层 |

这些表面按需分配，属于呈现状态，不写进原版图层或当成存档权威数据。640×480×4约1.17MiB，960×720×4约2.64MiB；迁移时要预算多份底图、文字层、上传快照和线程栈，不能只算一张输出纹理。

正文先写入高清源，再跟随原版逐字揭示复制相应矩形；一键全显、姓名前缀、清页、滑入滑出都必须同步。否则会出现“低清字还没出现，高清字已经全显”或旧字残留。

底图重建流程：

1. 按当前消息/选择框状态，绘制无文字底图。
2. 复制该底图，叠加原始低清文字，得到参考图。
3. 将参考图与真实原始画面比较。不同的像素可能来自日期、参数条、原生精灵等后绘制对象。
4. 将这些真实像素保留在底图中，并清除其覆盖范围内的高清字。比较时先检查整行；相同行不再逐像素循环。

遮挡像素映射当前使用 `x0=(x*3+1)/2`、`x1=((x+1)*3+1)/2`，Y同理；字形位置/尺寸则多用整数 `*3/2`。修改缩放比例时应重新定义取整规则，覆盖奇数坐标、边界和相邻矩形测试。

合成去重有具体前提：《鬼作》正文位于 y=404..457，按钮从 y=464 开始，二者应用相同滑动偏移，因此可以先画窗口/按钮，再叠加正文参考。另一游戏若按钮和文字相交，必须重新证明绘制顺序等价。

源层与 `present_shadow` 不一致，或处于信件、小说、部分消息淡化/转场时，当前代码保留原始画面。这是尚未接入的高清范围，**不能称这些模式已高清化**。上传失败则报错，不能把上传错误隐瞒成已正常显示。

## 4. 字体和描边的性能原则

本项目字体分工是用户指定的：外部OTF用于剧情正文/分支选择；历史、姓名输入等系统面板沿用HOS字体。另一游戏应单独确认，不能将所有 `draw_text` 统一换字体。

- CP932先解码成Unicode，再交给FreeType；不要把CP932字节当作Unicode码点。
- HOS/TTC字体显式选择合适的Unicode charmap。只成功打开字体不代表字符索引正确；过去出现过字格存在而字看不见的错误。
- 每个 `KFont` 独立缓存字形覆盖度和度量，键为码点、宽、高；当前上限1024字形、2MiB。位置和颜色不进入缓存键，换色不需要重新光栅化。
- 描边复用一次字形光栅化的结果：四方向1像素黑边用半Alpha，外侧2像素用四分之一Alpha，然后画正文。它不是FreeType轮廓描边器，像素半径以当前栅格为单位。
- 不能为描边在九个偏移位置各跑一次FreeType；曾由此造成明显卡顿。
- 字形缓存有可变LRU链表，`FT_Face`也有可变状态。同一字体实例不能被多个线程同时使用。

源码：[font.c](../runtime/font.c)、[高清合成入口](../runtime/bootstrap.c)。

## 5. 放大、锐化和颜色通道

当前正常GPU路径使用自有GLES2纹理，源640×480，以限幅Catmull-Rom双三次重建输出到960×720；底层仍利用硬件线性采样合并相邻权重。强度读取 `[Display] CASStrength`，范围0..100，也接受带小数点的0..1值。未设置/0表示双三次重建而不额外锐化，**不等于nearest模式**。CPU后备的0仍是双线性。

现有着色器在双三次重建之后，可选使用3×3邻域取min/max、四个正交邻居参与加权，权重含 `-0.2 × strength × amp`，最终限幅。项目历史记录称其CAS/RCAS；迁移时应称为“当前项目的限幅锐化实现”，不要仅凭名称或注释宣称与AMD官方实现逐式一致。

**已知实现边界：** GLES邻域偏移是一个源像素（`1/640,1/480`），CPU后备在放大结果的相邻输出像素位置取样；当前两者不是相同采样网格，也没有正式证明逐像素一致。当前GLES是一次pass中双三次重建和可选锐化，并非先生成目标尺寸中间纹理、再运行第二次RCAS；没有实现Lanczos。若下个项目要求严格的两阶段算法或官方一致性，应独立实现和验证。

颜色约定必须写清楚：

- 游戏 `KImage` 是BGRA字节布局。
- SDL路径使用 `SDL_PIXELFORMAT_BGRA32`，由SDL负责底层解释。
- 自有GL纹理用 `GL_RGBA` 上传**未交换的BGRA字节**，着色器每次采样用 `.bgr`恢复RGB，最终整屏输出Alpha=1。
- 这一不透明输出只适用于最终底图，不能直接套给透明精灵。
- 不借用SDL纹理的native handle进行自定义采样。SDL可能在自己的shader中交换通道，绕过它会使肤色变蓝、黄围巾变青。

底图有变化才复制/上传；未变化只重绘自有纹理。padded stride只比较有效行，不将行尾填充当像素。测试入口保留 `--raw-present` / `--raw`；Switch上设置nearest曾出现黑屏，实际采样效果需要用棋盘格/读回检查，不应只看API返回成功。

## 6. 混用SDL和GLES时的状态边界

自定义pass前先 `SDL_RenderFlush`，避免尚在SDL队列中的命令被随后修改的GL状态影响。保存并恢复当前实际修改的所有状态：

- current program、active texture unit、unit0的2D纹理绑定；
- viewport、ARRAY_BUFFER绑定、UNPACK_ALIGNMENT；
- attribute 0/1的 enabled、size、type、normalized、stride、各自buffer绑定及pointer；
- blend和scissor的启用状态。

传客户端顶点指针之前必须绑定 `ARRAY_BUFFER=0`。若SDL留下VBO，未解绑时裸指针会被当成缓冲区偏移。恢复属性时先恢复该属性原来的VBO，再恢复pointer，最后恢复全局ARRAY_BUFFER。

**过去的关键回归：** 后处理改写并禁用了顶点属性，却继续让SDL使用其缓存的渲染状态，造成高清文字、鉴赏、历史、存档界面消失或串图。只在下一帧“重新画一次”不能修复SDL缓存失配。

当前pass没有修改的状态不必无目的地全查；将来若增加FBO、scissor矩形、blend函数、额外纹理单元或其他属性，必须扩展保存恢复及测试。不要为减少glGet开销直接删除必要的状态恢复。

## 7. 缓存与局部上传

`KTextureCache`持有独立像素快照及持久SDL纹理：

1. 首次创建或尺寸变化，分配纹理并上传全图。
2. 精确比较每行的有效字节，查找首个和最后一个变化行。
3. 仅上传两者之间的整行矩形；没有变化则不调用上传API。
4. 上传成功后更新快照；失败清理缓存，使下一次完整重建。

擦字和透明度变化也属于变化，不能只标记“新增字形”。局部上传数据指针从首个变化行开始，pitch仍是原图stride。两个距离较远的改动可能上传中间未变的行；这是当前简单实现的已知取舍，不是精细脏矩形系统。

当前用于高清文字和原生CG画面。底图GLES也有独立整帧比较缓存。存档/音乐等其他UI不应据此宣称都已使用同一局部上传缓存。

历史、原生CG已证明输出完整不透明面板，所以跳过其背后的剧情高清合成和锐化。其他半透明面板必须保留底图；不能仅凭“打开了菜单”就跳过背景。

历史字体缓存、CG纹理缓存分别验证“静止时不新增光栅化”和“不每帧创建/销毁纹理”。缓存命中不代表整帧零CPU开销，仍有像素比较、绘制和部分合成。

## 8. 多核心分工与线程所有权

| 工作 | 当前Switch分配 | 说明 |
|---|---|---|
| VM、输入、SDL/GLES、音频混音 | 主线程原有调度 | 此补丁没有显式把主线程绑定core 0 |
| 语音解码 | core 1，优先级0x2c | 独立归档、PCM结果交接 |
| 当前帧高清合成准备 | core 2，优先级0x2c | 1MiB栈；不碰SDL/GLES |
| 图片解码 | core 2，优先级0x2d | 让当前帧准备优先；同核竞争仍需实测 |

“第三核”指core 2，不是core 3。不要为了图表占用率而启动空转线程。worker通过条件变量等待，没任务就休眠；主机测试用pthread，不模拟Switch的实际核心分配。

`present_worker`不是整个bootstrap的通用并发锁。提交后，worker独占当前runtime的呈现、选项和字体状态；`bootstrap_present_layers`可能更新选项页和字形缓存，不能当成纯只读函数。

主线程在这一小段重叠区只执行已审查的音频混音和SDL清屏；当前混音仅更新音频游标并读取设置，不碰字体和选项。任何后续新增的并发工作都要重新审查。主线程必须等待后才能：

- 读取准备结果，绘制其他可能使用字体的UI；
- 推进VM、处理输入、切选项/菜单、修改设置；
- 替换存读档runtime、释放字体/图层、退出程序。

任务仅准备当前帧，不能在未消费结果时再次提交；没有多帧队列，不故意增加一帧延迟。输出是runtime内部表面的借用指针，下一轮修改/销毁runtime前就必须用完。异常退出也先 `present_worker_clear` 等待任务、join线程，再释放runtime和SDL资源。

## 9. 帧率与性能证据

主循环请求VSync，并按 `SDL_GetPerformanceFrequency()/60` 的累计期限调度，余数累计避免长期截断漂移。目标上限60 FPS；负载过重时会低于60。20ms/15ms原生演出时钟仍由原有逻辑处理，不能为降负载随意减掉VM帧或改变原版动画结束条件。

固定主机检查点的结果：

| 检查 | 结果 | 不能推出什么 |
|---|---|---|
| 300次稳定HQ准备，对比bdc9a53 | 108.672ms → 62.990ms，约减少42% | 不是总CPU下降42%，也不是Switch实测 |
| raw/clean/overlay与附加遮挡后的哈希 | 前后完全一致 | 不覆盖全路线所有图层组合 |
| 100帧稳定底图 | 1次GL上传 | 不代表100帧只执行一次pass |
| 30帧稳定960×720文字 | 1次SDL上传 | 不代表所有合成都缓存住了 |
| CG面板12帧 | 1次纹理上传 | 变体切换/动画帧仍需更新 |
| 单行变化、擦除、底行变化 | 局部上传与全图上传的GPU画面一致 | 不验证所有驱动的性能 |
| 串行/worker、真实开场与选项、混音重叠、销毁等待 | 像素一致，TSan未报告竞态 | 不代替Switch长时间运行 |

后续实机测量应分开记录：VM/原生合成、准备任务总耗时、主线程等待时间、上传、GL绘制、VSync等待；同时记录画面/设置/频率模式。第三核负载升高只表示工作转移，不能单独证明帧时间改善。

## 10. 建议迁移顺序

1. 先确定另一游戏原始分辨率、像素格式、Alpha语义、编码、原生文本矩形和图层所有权；保留可运行的原始呈现基线。
2. 单独接放大pass，先用纯色、棋盘格、不同stride和半透明边缘检查颜色、方向、采样与裁剪；先不接线程。
3. 用实际SDL renderer，在pass后连续绘制文字、CG/音乐/视频/回想、历史、存读档，确认没有SDL状态污染。
4. 接高清伴随文字，从普通正文和选择框开始；覆盖逐字、瞬显、清页、姓名前缀、菜单覆盖和原生精灵遮挡。信件/小说单独实现。
5. 接字形缓存、纹理复用及变化行上传；比较局部上传与全图上传，包括透明擦除和换runtime。
6. 先在单线程下减少重复合成，拿固定状态与优化前逐像素对比。
7. 再接worker，明确独占区和可重叠工作。跑串行/并行对比、TSan、提交后退出/读档切换检查。
8. 最后交叉编译、打包、实机测帧时间与稳定性。若GPU路径未启用、进入CPU后备，应先查清原因再讨论多核收益。

每一步留下独立commit及可复现结果。`bdc9a53`适合追溯正确性修复，`9f238f3`适合查看优化差异；二者包含《鬼作》专用代码，不能无审查地整体cherry-pick到另一游戏。

## 11. 当前项目的复验命令

在仓库根目录运行，原始 `鬼作/` 保持只读：

```sh
./build-host.sh
./test-host.sh 鬼作

tools/test_present_gles.sh local/fonts/arshanghaisonggbpro_lt.otf 鬼作

mkdir -p local/hires-note-check local/present-note-bench
build/kisaku-bootstrap-test 鬼作 local/hires-note-check --hires
build/present-benchmark 鬼作 local/present-note-bench

./build-switch.sh
python3 tools/package_sd.py 鬼作
shasum -a256 build-switch/kisaku.nro 交付/SD卡根目录/switch/kisaku/kisaku.nro
```

`tools/test_present_gles.sh`默认依赖macOS Homebrew Mesa路径，可用 `KISAKU_MESA_PREFIX`更改；脚本当前库名为`.dylib`，移到其他主机要适配。实际renderer是Mesa offscreen/llvmpipe，不是Switch GPU。测试的“逐像素一致”主要针对状态恢复、叠加和上传；不代表CPU/GPU非零锐化强度或AMD官方算法一致。

线程检查：按 `build-host.sh` 中 `tests/kisaku_bootstrap_test.c` 的同套源文件和链接库构建，改用 `-O1 -g -fsanitize=thread`，输出例如 `build/present-worker-tsan`，再运行：

```sh
mkdir -p local/present-thread-check
TSAN_OPTIONS=halt_on_error=1 build/present-worker-tsan 鬼作 local/present-thread-check --hires
```

不要只检查编译成功或软件renderer截图。至少检查一次**实际SDL/GLES管线中的菜单+高清文字**，再单独保留Switch实机“已验证/未验证”记录。

## 12. 仍待补齐或不能直接推广的部分

- 信件/小说/部分淡化模式与旧档当前句的完整高清重建。
- 目标游戏自己的原生接口、回想目录、菜单图集、动画次序和完成状态。
- CPU/GPU锐化采样统一、正式官方算法一致性验证，以及Lanczos；输出分辨率两阶段已在第14节接通。
- 任意输出尺寸、非1.5倍缩放、renderer重建/设备状态变化的完整适配。
- 所有菜单通用脏矩形、整个呈现结果的跨帧缓存；目前只实现本笔记明确列出的缓存。
- Switch实机核心负载、帧率、耗电、热稳定性和长时间运行。

更完整的历史证据见 [porting.md](porting.md)，其他游戏功能欠项见 [remaining-work.md](remaining-work.md)。


## 13. 2026-09-23：GPU双三次放大更新

用户希望背景、立绘进一步减少双线性放大的模糊，同时避免增加CPU高清合成。此次只改变正常GLES底图放大；高清文字继续独立叠加，模态面板自己的SDL绘制、nearest参考和CPU后备不跟着改算法。

### 算法与实现

`tools/present_gles.inc` 使用Catmull-Rom插值。源坐标取 `p=uv/texel−0.5`，以 `floor(p)` 定位源像素，保持像素中心对齐。每轴四个权重为：

```text
w0 = −0.5f + f² − 0.5f³
w1 = 1 − 2.5f² + 1.5f³
w2 = 0.5f + 2f² − 1.5f³
w3 = −0.5f² + 0.5f³
```

将正的中间两项合并为 `wm=w1+w2`，采样位置按 `w2/wm`偏移，利用硬件双线性完成该对权重，得到3×3共9次采样的双三次重建。思路参考：[Bicubic Filtering in Fewer Taps](https://vec3.ca/bicubic-filtering-in-fewer-taps/)；另可参阅作者公开实现：[MJP Catmull-Rom sampling](https://gist.github.com/TheRealMJP/c83b8c0f46b63f3a88a5986f4fa982b1)。本仓库实现还增加了自己的限幅与既有BGRA解释。

额外读取中央2×2的四个源像素，将双三次结果限于这四点逐通道min/max之间，抑制高反差边界的过冲。该限幅有意改变未经限幅的标准插值结果；主机参考也明确应用同一中央2×2限幅。片元运算使用highp，避免640×480坐标下的小数精度不足。纹理仍CLAMP_TO_EDGE、GL_RGBA存储BGRA字节，每次采样仍`.bgr`；未借用SDL纹理。

`CASStrength>0`才执行现有8邻域额外锐化，设置含义与强度范围保留，但输入中心值已变为双三次结果，所以视觉结果有意改变。CAS为0也能获得新的双三次重建。

### 开销边界

- CPU继续上传原尺寸640×480，高清字层仍960×720；原有上传缓存、第三核准备、60 FPS目标都保留。
- 没有新增CPU放大循环、线程、目标分辨率中间FBO或额外帧队列；新增成本主要是GPU片元采样和运算。
- 源码逻辑路径为13次采样（9次重建+4次限幅）；非零锐化时共21次。旧shader源码为9次采样。该数字是算法中的采样表达式数量，不等于GPU实际内存事务次数，也不能换算成耗时比例；编译器对分支的处理需要实机测量。
- 程序/着色器初始化成本和GPU驱动等待仍在主线程，因此不能承诺CPU观测占用绝对不变。
- CPU后备仍为双线性/原有锐化；它与新GPU路径明确不同。不应在GPU初始化失败后把后备画质当成新算法已生效。
- 此pass作用于原始底图，包括剧情背景/立绘与标题。原生CG鉴赏等独立模态仍保留自己的SDL呈现；本次没有把全部图片菜单统一送入双三次pass。

### 验证与复验

`tests/present_gles_render_test.c`新增独立CPU参考：按距离形式的分段三次核累加完整4×4的16点，不照搬shader的9次合并采样。使用彩色周期线、渐变、细棋盘格、边缘夹取及非255源Alpha，遍历960×720输出，检查通道、相位、中央限幅、Alpha和源图不变。

本机Mesa实际GLES结果：最大参考误差1/255，584978个颜色通道与双线性结果相差超过2/255，证明并非仍在执行双线性。六类实际SDL菜单、高清文字叠加与文字局部上传的回归同时通过。对比原始标题的线性/双三次截图，也检查了色彩、发丝/面部轮廓、菜单图片文字及画面边缘。

```sh
tools/test_present_gles.sh local/fonts/arshanghaisonggbpro_lt.otf 鬼作
```

可选真实图片截图：为上面脚本额外设置 `KISAKU_FILTER_SHOTS=local/filter-check`，会生成 `local/filter-check-linear.bmp` 与 `local/filter-check-cubic.bmp`。这些图片含原版素材，不提交仓库。

日志：`local/bicubic-gles.log`、`local/bicubic-preview.log`；截图：`local/bicubic-preview-linear.png`、`local/bicubic-preview-cubic.png`。Switch编译和产物哈希见porting.md本轮记录。**尚未测量Switch实机GPU增幅、CPU等待、帧率、发热和实际清晰度收益。**

## 14. 2026-09-23：输出分辨率边缘锐化（两阶段，非FSR）

用户选择继续使用现有放大，并增加边缘自适应与输出分辨率锐化。本节覆盖第13节关于“没有中间FBO/额外pass”的旧描述；不是FSR、CAS或RCAS官方实现。

### 管线及设置

- 第一阶段：640×480 BGRA源纹理 → 有中央2×2限幅的Catmull-Rom → 输出viewport尺寸的独立RGBA8纹理。通常为960×720；窗口变化时重新分配，旧纹理销毁。
- 第二阶段：按输出纹理的一像素步长读取3×3邻域，计算亮度细节和边缘强度，锐化后写回原framebuffer。然后由原流程叠加高清文字和SDL界面。
- `[Display] EdgeStrength=55` 为默认强度（0..100百分比）。`EdgeStrength=0`选择上一版单阶段双三次＋`CASStrength`路径；raw nearest仍单独保留。
- 新模式第一阶段强制关闭旧CASStrength锐化，避免两次锐化。EdgeStrength与CASStrength彼此独立，旧设置保留。该新增项是设置文件扩展，不向原版CConfig伪造新控件。
- CPU后备未新增此算法；GPU初始化或中间目标失败时沿用已有后备路径，不能把后备当成此效果已生效。所有原版内部表面、存档缩略图、命中坐标不变。原生CG等独立模态图片依然未统一经过该pass。

### 运算与保真边界

亮度 `L=dot(rgb,(0.299,0.587,0.114))`（直接作用于当前编码颜色，不宣称线性光处理）。取3×3亮度的min/max和均值：

```text
gate = smoothstep(0.02, 0.16, maxL-minL)
delta = clamp((L-meanL) * (EdgeStrength/100) * 0.9 * gate, -6/255, 6/255)
delta = clamp(L+delta, minL, maxL) - L
outRGB = clamp(rgb + delta, 0, 1)
```

平坦/微小纹理不增强，高反差轮廓渐进增强；仅施加共同亮度增量，减轻独立RGB锐化带来的色偏。饱和通道仍可能因0..1裁剪改变色度，不承诺严格色彩不变。6/255限幅抑制过冲，不能保证任何素材都无白边，也不能恢复原图没有的细节。

### GL生命周期与开销

- 额外RGBA8目标960×720约2.64MiB（驱动实际分配可能更大），以及一个FBO；不增加CPU高清图像处理、读回或帧队列。
- 第一阶段保留原BGRA `.bgr`，中间纹理实际为RGBA，第二阶段读取`.rgb`。FBO纹理纵向与上传源不同，第二阶段使用单独UV避免上下颠倒。
- 在SDL_RenderFlush之后保存GL状态，包括FRAMEBUFFER_BINDING。所有成功/失败出口恢复原framebuffer、纹理、program、viewport和顶点属性等状态；目标完整性检查失败返回错误。退出统一释放纹理/FBO。
- 第二阶段中心＋邻域为10个采样表达式（中心重复包含在邻域内，驱动可能消除）；同时增加一次目标写入和一次绘制提交。主要增加GPU及带宽工作，但CPU驱动提交/等待并非零；实机GPU占用和60FPS稳定性仍须测量。
- 既有源上传缓存、高清文字局部上传、工作线程分工和60FPS目标保持。

### 验证

`tools/test_present_gles.sh`覆盖真实Mesa/GLES两阶段、独立CPU参考25/50/75/100强度、输出尺寸改变、旧模式恢复、弱纹理不增强、原有高清文字/六种菜单绘制。状态夹具额外检查非默认framebuffer恢复以及FBO不完整后可重试。截图环境变量现在同时导出 `*-linear.bmp`、`*-cubic.bmp`、`*-edge.bmp`。

日志使用 `local/edge-gles.log`、`local/edge-host.log`、`local/edge-switch.log`；本次实机性能和清晰度收益未验证。最新产物哈希见porting.md。

## 15. 2026-09-23：可恢复的 FSR 1.0 试用模式

用户反馈第14节提升较轻微，明确要求试用FSR1并可恢复。新版默认 `PresentFilter=fsr1`：源640×480 → FSR1 EASU → 输出尺寸RGBA8 → FSR1 RCAS → 高清字层/SDL菜单。第14节两阶段滤镜及旧CASStrength路径仍保留；两者不叠加。原图、存档及脚本表面均不变，CPU后备和独立模态图片的范围限制继续适用。

### 公式与GLES2适配边界

算法来自 AMD `ffx_fsr1.h` v1.20210629，官方仓库：
https://github.com/GPUOpen-Effects/FidelityFX-FSR/blob/master/ffx-fsr/ffx_fsr1.h

此次核对文件SHA256 `93c3922362ea7fc99cbcc698ca30c98de4f8c246d1fbb0b09e015ddef38ce3a5`。移植代码在 `tools/fsr1_gles.inc`，MIT声明保留在文件及会随交付复制的THIRD_PARTY.md。

- EASU为12点方向/各向异性自适应Lanczos近似核：四个十字亮度梯度按双线性权重估计方向和长度、归一化后旋转/拉伸核，按原式计算负瓣和权重，最后用中央2×2逐通道min/max去振铃。
- RCAS读取上/下/左/右/中心五点，用四邻点逐通道min/max推导限制器，并取跨通道共同负权，限值 `-(0.25-1/16)=-0.1875`。本版不启用官方可选RCAS_DENOISE，不叠加上一版±6/255限制器。
- GLES2没有textureGather/texelFetch，使用显式纹理像素中心采样。EASU源仍BGRA字节经`.bgr`，RCAS中间纹理为RGBA经`.rgb`，两阶段维持原有FBO纵向处理。
- 使用FP32 highp和普通除法/逆平方根替代原版位级近似，零分母加保护，因此这是FSR1公式的GLES2适配，不宣称与AMD SDK位精确一致。RGBA8中间结果含8位量化。
- 新旧模式共用输出纹理/FBO及完整GL状态保存恢复，不新增CPU高分辨率合成、不新增帧队列。EASU12点+RCAS5点的表达式数量不代表实际GPU耗时；Switch负载和帧率须实测。

### 切换与恢复

运行时在游戏 `AI6WIN.ini` 后读取存档目录 `kisaku-runtime.ini`，后者覆盖前者。可在SD卡 `switch/kisaku/saves/kisaku-runtime.ini` 现有 `[Display]` 节修改以下键（不要覆盖其他设置）：

```ini
PresentFilter=fsr1
FSRSharpness=90
```

`FSRSharpness=0..100`线性映射到官方sharpness的2..0 stops：`RCAS multiplier=exp2(-2*(1-strength/100))`。默认90即0.2 stops；100最强，0是2 stops较弱锐化，**不是关闭**。畸形数值用默认90。关闭FSR请设 `PresentFilter=edge`，继续使用 `EdgeStrength` 默认55及旧设置；`EdgeStrength=0`恢复单pass双三次/CAS路径。设置更改后重启游戏。

已保存修改前 `afbc2d9` NRO：`交付/恢复版本/afbc2d9/kisaku.nro`，SHA256 `50afeb4c852842693542467855b2dcb4d88612e010e90fe211649c8076415cc0`。可直接覆盖SD同名主程序恢复，无须动game/saves；如果从兼容入口启动，将旧NRO也覆盖该入口文件名。该备份是本地生成物，不提交git。

### 验证证据

- `tools/test_present_gles.sh`：实际Mesa GLES EASU中间纹理对独立double标量参考、RCAS整面参考均最大误差1/255；测试图与旧edge有1838252通道变化，切回edge逐字节一致。
- 平坦颜色下高清文字叠加与CG/音乐/视频/场景/历史/存读档SDL菜单回归；FBO状态及既有双三次/edge测试继续保留。
- 截图环境变量额外输出 `*-fsr1.bmp`，便于与 `*-edge.bmp` / `*-cubic.bmp` 同源对照。
- 日志 `local/fsr-gles.log`、`local/fsr-host.log`、`local/fsr-switch.log`，本次精确产物与主机执行结果见porting.md。未做Switch实机视觉/负载/60FPS验证，也不保证FSR对每张旧图都更好。

用户随后要求效果再强一点，最终默认从80上调90（0.4→0.2 stops），只调整RCAS系数，不增加pass或纹理；日志和最终交付以90为准。


## 16. 2026-09-23：根据实机反馈恢复FSR初版强度

用户反馈增强版噪点增多，要求恢复FSR初版。当前默认 `FSRSharpness=80`（0.4 stops），撤销第15节后续的90（0.2 stops）；保留FSR1 EASU＋RCAS，不切回edge模式，不新增降噪或改变原图。RCAS系数由约0.87055恢复约0.75786，减弱对细密纹理/噪点的增强。无新增pass、纹理或CPU工作。

`bootstrap_fsr_strength`缺省和非法值回退都恢复80，测试夹具与独立RCAS参考同步恢复80。显式用户配置仍优先：如果在 `switch/kisaku/saves/kisaku-runtime.ini` 的 `[Display]` 节手动写过 `FSRSharpness=90`，应改成80或删除此键后重启；程序不擅自改写存档设置。`PresentFilter=edge`与afbc2d9旧NRO恢复途径保留。

这是用户实机观察驱动的参数回调，不将本机GPU回归当作实机降噪效果验证。当前产物、验证命令和SHA见porting.md本轮记录。

## 17. 2026-09-23：整页信件/小说文字的高清合成

`runtime/present_page.inc` 为整页文字增加独立的960×720源字形与已显示字形；24像素字形按36像素重新光栅化。字体选择仍走原有信件/小说Mincho分支，没有将用户OTF扩展到系统面板。游戏layer0/layer1、文字游标、原始01.bin、存档与脚本执行仍保持原尺寸及原数据。

- 在文字指令执行时同时绘制高清字形，记录640×480文字表面影子；外部脚本修改未捕获的文字像素时拒绝使用不完整的高清层，保留原始绘制。
- 信件逐行显示按原版24像素行高、15ms时钟和`min(字形Alpha, phase-mask)`裁剪高清Alpha。旧小说兼容路径沿用自身26像素行高和0..128遮罩比例；不能把该兼容夹具视为《鬼作》真实小说路线验证。
- 每次原生文字合成后保留原始画面快照；present阶段比较最后可见像素，后画的日期/其他精灵保留，其覆盖区域对应的高清字形被遮掉。没有改动640×480内部表面。
- 完整显示、后续段落、隐藏/恢复、清页/退出和信件读档重建均接通；隐藏/清页/退场的整屏交叉淡化过程仍走原生合成帧，淡化结束后使用高清文字。尚未完成这些淡化过程本身和CMesFadeSprite的高清叠层，不应写成所有文本效果完成。
- `runtime_viewer` 将信件/小说纳入已有core2呈现准备，复用现有文字纹理、变化行上传和FSR80背景路径。不增加GPU后处理pass。整页辅助缓冲按需增加约8.8MiB，退出整页模式释放；不是增加内部画面分辨率。CPU新增字形绘制、Alpha遮罩和快照复制，实机耗时没有测量，不能声称零开销。

验证：三档速度下高清开/关双运行时逐帧原始像素一致；线程/顺序输出一致；后画像素遮挡、清页与隐藏恢复通过。真实memo.mes四个检查点从新标题读档后高清字形逐字节一致，继续段落及退出RGB差异0。真实SDL/GLES信件页有2585个非透明字形像素，12帧保持一次上传并与独立完整上传逐字节一致。ASan/UBSan专项通过。构建、完整回归与交付哈希见porting.md；Switch实机仍未验证。

## 18. 2026-09-23：淡化过程保留高清文字

覆盖第17节的整屏淡化与 CMesFadeSprite 待办。原始640×480表面、脚本/存档和15ms时钟保持不变；raw-nearest仍直接显示原始帧。

`present_fade.inc` 在清页/退出破坏字图前保存高清字形和无字底图，信件隐藏/恢复、清页/退出以及旧小说兼容路径清页/退出都保持960×720字形。逐行遮罩仍沿用第17节实现。淡化计算按“两个完整端点先各自合字，再按原进度混合”，避免只缩放字形Alpha产生底色光晕；字形覆盖的少量像素用双线性采样两个底色并生成覆盖片，其余背景继续现有FSR80。覆盖片的底色不经过FSR锐化，因此不声称与把两个端点各自执行FSR后混合逐像素相同。

`CMesFadeSprite` 保存独立高清源字、旧字、新字和原表面影子，按原生居中矩形搬运；type1交叉淡化、type2逐行Alpha和type3的03.bin阈值全部作用在高清字形覆盖率上。保留已有文字、隐藏与黑色遮罩使用原时钟/透明度，结束时只提交对应矩形；未捕获的脚本写字仍触发原帧安全路径。旧/新文字重叠使用有界整数权重，避免极低Alpha下的颜色溢出。

- 主线程只记录端点/原帧快照，像素呈现计算放入已有core2 worker；GL/SDL仍在主线程。未增加GPU pass，继续变化行纹理上传。
- 整页淡化临时表面约9.96MiB，淡化结束释放；CMesFade高清表面约10.25MiB，随原生释放动作销毁。新增CPU混合/快照开销未在Switch测量，不能声称帧率或负载已达标。
- 原始像素开关高清对照、三种CMesFade×三档速度、信件隐藏/恢复/清页和旧小说清页、ASan/UBSan专项均纳入验证。实际SDL/GLES信件隐藏/恢复14帧，完整上传与增量上传逐字节一致；菜单、字体和FSR既有GPU回归一并执行。

本次Switch编译与交付哈希见porting.md；实机视觉、帧时、全部路线仍未验证。

## 19. 2026-09-23：NVTEGRA 视频硬解接入（不修改 Mesa）

Switch 的 `KVideo` 默认选择 AUTO：查询 FFmpeg 的 `nvtegra` 设备类型及当前 codec 的硬件配置，创建设备并在 `get_format` 中选择其像素格式。主机默认仍为软件解码。沿用已安装的 devkitPro FFmpeg，其静态库包含 MPEG-1 NVTEGRA 后端；没有修改 Mesa、GL、FSR 或游戏内部表面。

硬件帧在 `avcodec_receive_frame` 后立即通过 `av_hwframe_transfer_data` 下载，再复制时间戳/颜色属性并释放硬件帧引用。现有 swscale 转 BGRA、MOV 区间和音频路径保留。循环缓存只持有 CPU 帧，仍限128帧/64MiB，避免缓存耗尽 NVDEC 表面池。硬解不等于零拷贝：下载、颜色转换及图层合成仍有 CPU/内存带宽开销，未接入 VIC 转色或 GPU 直接采样。

- 缺后端、codec不支持、设备初始化失败、硬件格式不可用或codec打开失败时回退软件，并记录原因。codec打开失败只重试一次软件路径。播放中解码/下载失败明确报错退出，不宣称支持无缝中途切换。
- `saves/kisaku-runtime.ini` 的 `[Runtime]` 可设置 `VideoDecoder=software` 强制软件；`auto` 恢复自动选择。需重启，配置在工作线程启动前读取，不改变现有核心分配。
- 2026-09-23 按用户要求关闭 `saves/video-decoder.log`：运行时以 `kvideo_set_log_path(NULL)` 禁用文件输出，不再创建、截断或追加该文件；已有旧日志不自动删除。硬解 AUTO、软件回退和内存帧计数不变，诊断仍可输出到 stderr。只有实际硬件帧计数或 `NVTEGRA active: first hardware frame downloaded` 才能证明产出硬件帧，旧日志标题和设备创建成功均不能证明。
- 适配器故障注入覆盖设备/格式/打开失败、下载失败、时间戳、硬件引用释放和CPU缓存。真实VSD比较软件与主机AUTO回退的RGB/PCM，覆盖带音频播放片段、无音频循环、区间跳转、提前关闭；不能把主机测试称为NVDEC验证。

构建/验证及产物哈希见porting.md。Switch硬解可用性、色彩一致性、音画同步、CPU负载下降幅度和长时间设备资源稳定性均未实机验证，不承诺性能百分比。
