# 回看原生接口核对（2026-09-22）

本轮只完成记录容器接口修正，**①完整回看仍未完成**。原始输入只读，未启动 PC 程序。汇编摘录在 `local/backlog-record-native.asm`，来源 `鬼作/AI6WIN.exe`。

## 鬼作分发表

`005061c0` 的 case `0x17`（十进制23）构造 `CFuncBackLog`（`004fe6c0`），调用 `004fe5f0`。case `0x1d`（十进制29）构造 `CFuncWait`（`004f2870`），调用 `004f2970`。旧 `29/0` 注释将两者混淆，不能据此把等待当作回看初始化。

| 调用 | 原版链路 | 本次结果 |
|---|---|---|
| 23/0 | 4fe5b0 → 4fe580 → 500c10 | 追加指定数量空槽；分配/范围失败保留参数和旧状态 |
| 23/1 | 4fe170 → 500960 → 40c550 | 当前索引归 -1；清命令长度、字符串和语音标志；保留分配 |
| 23/4 | 4fe3b0 → 4fe100 → 500900 | 返回非空命令向量数量，不是容量 |
| 23/6 | 4fe270 → 4fe0b0 → 5008c0 / 41cd60 | 返回指定槽语音标志，越界返回0 |
| 29/0 | 4f2900 → 4e2480 → 4e4640 | 弹出毫秒数和更新标志；零时长立即返回；非零时长明确报未支持并保留参数 |

`40c550+0x1d` 明确写零到对象 `+0x38`，旧实现“保留 flag”不符合鬼作。23/4、23/6 返回值使用已消费操作数空间，不额外分配栈。

## 后续必须完成

- 原生命令流记录/重放、逐段文字状态和多语音；目前前端仍读64条扁平文本/单语音，并且存档扩展仍是旧格式，不能称完整回看。
- 23/5、23/7 的原生记录/文本引用返回及23/8的确切调用约定；不能将 PC 指针直接当移植 VM 整数使用。
- 23/2、23/3 与前端记录生命周期统一；旧记录器仍未完整记录绘制命令。
- 29/0 非零等待和29/1可跳过等待：需要核对事件泵、更新标志、输入隔离、时钟。撤回空操作可能使以前错误放行的路径明确停住，这是修正验证边界，不代表新路径已通。
- 实机、真实场景完整回放仍未验证。

## 验证入口与日志

- `./build-host.sh` → `local/backlog-record-build.log`
- `./test-host.sh 鬼作` → `local/backlog-record-final-test-host.log`
- `./build-switch.sh` → `local/backlog-record-switch.log`
- `python3 tools/package_sd.py 鬼作` → `local/backlog-record-package.log`

新增 `test_backlog_records` 覆盖追加保留、非空计数、语音查询、负数/越界、清空后语音不残留、缺失/字符串/超限参数不消费，以及29/0不能错误充当无参数初始化。最终运行结果见 `reports/porting.md` 同日“回看容器接口核对”节。
