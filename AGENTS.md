# 协作约定

- 默认简洁中文；已授权范围内自主实现、排错及必要验证。
- 本工程当前只完成基础启动预览，不得把参考项目的可玩性和实机结果当成《鬼作》的验证。
- `鬼作/` 为原始输入，保持只读；运行生成物放 `local/`、`build/`、`build-switch/`、`交付/`。
- 不提交原版素材、存档、EXE、参考仓库副本及交付数据包。不自动推送。
- 未支持的原生调用必须保留参数并明确报错；不得为推进脚本把未知接口当作成功。
- 参考工程中的地址注释仅适用于参考游戏；《鬼作》已核对地址见 `reports/porting.md`。

## 当前移植范围

- 先完整实现日文原版，汉化补丁暂不处理。
- 用户要求静态反编译实现；不再启动或运行 PC 版程序。
- 31/30 是 CNormalSelect 选项接口，并非 CTitle；旧记录中该名称需纠正。

## 当前实现与反编译入口

- `local/decompiled/` 已导出 5,307 个已识别函数，文件按 VA 命名；`functions.json` / `summary.json` 是索引和摘要。伪代码类型和间接调用须与汇编核对。
- 静态工具见 `tools/decompile_r2.py`、`tools/native_classes.py`；本地 r2ghidra 插件在 `local/r2ghidra-build/build/`，不属于运行时依赖。
- 日文固定 CP932。raw 区 07/08/09、11/12/13 指令与局部赋值 10 已实现；VM 借用的 raw 指针需要随 bootstrap 存储变化更新。
- 已核对 19/4/5/6/7/8/9 图层复制、Alpha 混合、填充、矩形减暗和 Alpha 写入、31/30/0 初始化及动作1选择返回、31/40 CFuncExec 翻页、31/520/0/1/3/4/6/7/10/12 的资源加载、轨道状态、轨道重置、空闲同步、首帧绘制、独立 20ms 时钟、参数消费与独立动画停止、31/10/2 窗口已经隐藏的分支；扩展动画默认源层按原版构造器为 layer 8，原生事件队列仍保留显式报错。31/521 双页蓝色遮罩转 Alpha、31/522 四窗口精灵 0..5 模式、31/523 模式 0/1 的 Sungeki 图层绘制链已接通；其余未知扩展继续保留参数报错。
- 当前启动经过 160 次调用、481 帧，到达日文 CTitle 菜单（31/110，mode 0），返回系统变量 18。尚不能进入完整剧情。
- 31/1011 动作 0/1 静态提取媒体表（1554/72 条及 189 条链接，保留重复键）；构建依赖 Python 3 和已校验哈希的日文 EXE。31/10/3 已按 481df0 重载皮肤。
- 31/528/24 已按 4beab0 清空日记两组 96 槽、重绘 608×1936 表面并重置滚动范围。31/528/10/0 参数窗口前三行和 49f5a0 的第四项/两个计数器隐藏表面重绘已接入，特殊标记 31/528/25,26 已接通。
- 31/612/2 已实现（bowling.h）：按 4cfd20 释放 39 个自有槽，保留 6 个借用图层，先注销再销毁登记对象，清空当前对象及结束状态。重复释放安全，未知所有权先报错且保留参数。创建/运行保龄球的动作 0/1 仍不支持。
- CLetter 31/525/2 已实现三个私有表面、画面保存与淡化、消息窗口重置。letter.inc 采用原版分别取整的 /255 混色；Display/EffectSpeed 三档已测，其他 CLetter 动作仍未支持。
- 31/524 的 0/1 已按 timepart.akb 的 layer7 接通三段状态图绘制与清理；29/30 继续实现四组精灵及独立标记的可见状态保存/恢复（overlay_state.h），包括重复暂停覆盖快照、恢复仅显示先前可见对象。
- 31/1010 仅未选择媒体流分支、31/1011 清空/设置背景名、31/528/23 辅助窗口已隐藏分支已核对；31/528 action 9 按 4fa9e0 默认分支消费 action/sub，保留日文 liblary 路径的八个前置参数。14/11 按 5080a0 合并进度，14/3..6 恢复 byte/word/raw/bank1；flags.c 根据 FLAGINI.MES/HAGE_FLAGINI.MES 为缺失的 0..201 槽生成新游戏初值，损坏存档不覆盖。
- `交付/SD卡根目录/` 已同步本轮构建。主入口为 `kisaku.nro`，`kisaku-preview.nro` 是相同内容的兼容文件名；仍是开发预览，不是完整游戏。

- 新游戏主机探针（只运行移植 VM，不启动 PC 程序）已推进到 2932 次调用、1820 帧，走过 `31/520/10` 与 `19/7` 后停在 `liblary.lib` 脚本 `0x1322` 的 `31/43`（`0x4f95a0`，参数保留报错）；该接口尚未实现。后续仍需沿脚本静态核对，不能把探针推进当成可玩验证。
