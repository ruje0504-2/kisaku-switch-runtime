# 协作约定

- 默认简洁中文；已授权范围内自主实现、排错及必要验证。
- 本工程当前只完成基础启动预览，不得把参考项目的可玩性和实机结果当成《鬼作》的验证。
- `鬼作/` 为原始输入，保持只读；运行生成物放 `local/`、`build/`、`build-switch/`、`交付/`。
- 不提交原版素材、存档、EXE、参考仓库副本及交付数据包。不自动推送。
- 未支持的原生调用必须保留参数并明确报错；不得为推进脚本把未知接口当作成功。
- 参考工程中的地址注释仅适用于参考游戏；《鬼作》已核对地址见 `reports/porting.md`。

## 当前移植范围

- 先完整实现日文原版，汉化补丁暂不处理。
- 用户要求静态反编译实现；不再启动或运行 PC 版程序。
- 31/30 是 CNormalSelect 选项接口，并非 CTitle；旧记录中该名称需纠正。

## 当前实现与反编译入口

- `local/decompiled/` 已导出 5,307 个已识别函数，文件按 VA 命名；`functions.json` / `summary.json` 是索引和摘要。伪代码类型和间接调用须与汇编核对。
- 静态工具见 `tools/decompile_r2.py`、`tools/native_classes.py`；本地 r2ghidra 插件在 `local/r2ghidra-build/build/`，不属于运行时依赖。
- 日文固定 CP932。raw 区 07/08/09、11/12/13 指令与局部赋值 10 已实现；VM 借用的 raw 指针需要随 bootstrap 存储变化更新。
- 已核对 19/4/5/6/7/8/9 图层复制、Alpha 混合、填充、矩形减暗和 Alpha 写入、31/30/0 初始化及动作1选择返回、31/40 CFuncExec 翻页、31/520/0/1/3/4/6/7/10/12 的资源加载、轨道状态、轨道重置、空闲同步、首帧绘制、独立 20ms 时钟、参数消费与独立动画停止、31/10/2 窗口已经隐藏的分支；扩展动画默认源层按原版构造器为 layer 8，原生事件队列仍保留显式报错。31/521 双页蓝色遮罩转 Alpha、31/522 四窗口精灵 0..5 模式、31/523 模式 0/1 的 Sungeki 图层绘制链已接通；其余未知扩展继续保留参数报错。
- 当前启动经过 160 次调用、481 帧，到达日文 CTitle 菜单（31/110，mode 0），返回系统变量 18。尚不能进入完整剧情。
- 31/1011 动作 0/1 静态提取媒体表（1554/72 条及 189 条链接，保留重复键）；构建依赖 Python 3 和已校验哈希的日文 EXE。31/10/3 已按 481df0 重载皮肤。
- 31/528/24 已按 4beab0 清空日记两组 96 槽、重绘 608×1936 表面并重置滚动范围。31/528/10/0 参数窗口前三行和 49f5a0 的第四项/两个计数器隐藏表面重绘已接入，特殊标记 31/528/25,26 已接通。
- 31/612/2 已实现（bowling.h）：按 4cfd20 释放 39 个自有槽，保留 6 个借用图层，先注销再销毁登记对象，清空当前对象及结束状态。重复释放安全，未知所有权先报错且保留参数。创建/运行保龄球的动作 0/1 仍不支持。
- CLetter 31/525/2 已实现三个私有表面、画面保存与淡化、消息窗口重置。letter.inc 采用原版分别取整的 /255 混色；Display/EffectSpeed 三档已测，其他 CLetter 动作仍未支持。
- 31/524 的 0/1 已按 timepart.akb 的 layer7 接通三段状态图绘制与清理；29/30 继续实现四组精灵及独立标记的可见状态保存/恢复（overlay_state.h），包括重复暂停覆盖快照、恢复仅显示先前可见对象。
- 31/1010 仅未选择媒体流分支、31/1011 清空/设置背景名、31/528/23 辅助窗口已隐藏分支已核对；31/528 action 9 的旧默认返回映射错误已撤回：真正入口为 4fc080 case 9 -> 4a00f0，需要参数变化、AX 同步和音效；当前保留全部参数报错。4fa9e0 属于 31/10。14/11 按 5080a0 合并进度，14/3..6 恢复 byte/word/raw/bank1；flags.c 根据 FLAGINI.MES/HAGE_FLAGINI.MES 为缺失的 0..201 槽生成新游戏初值，损坏存档不覆盖。
- `交付/SD卡根目录/` 已同步本轮构建。主入口为 `kisaku.nro`，`kisaku-preview.nro` 是相同内容的兼容文件名；仍是开发预览，不是完整游戏。

- `31/43` 已按 `4f95a0 -> 485f40` 核对为 CMesFadeSprite；动作 0/1/2 已实现私有表面初始化、释放、透明清屏与系统变量 42..45 重置。动作 3 的 type=1 交叉淡化、动作 4 隐藏消息、5/6 黑色遮罩淡入淡出已实现；动作 3 的 type=2/3 仍保留参数报错。参数窗口剩余次数已按原版坐标表纠正为 x=534/542，恢复逐像素断言。
- 撤回错误的 31/528/9 空操作后，可信的新游戏探针停在 772 次调用、1168 帧、63 次文本的 `liblary.lib+0x6949`，栈深 73；动作 9 后七个参数按弹出顺序为 100、1005、1000、1000、1000、0、1。此前 3417/3509 次调用返回标题及其他后续路径都越过了错误空操作，不能作为正确流程证据。
- `vm.c` 的操作数栈已改为可扩容容器：原版 `4b7120 -> 46f390 -> 402720 -> 402c30` 压入返回值并扩容，函数返回未统一清栈；不能删除对话返回值。新增 10000 项混合整数/借用字符串、扩容时自引用 push、完整逆序 pop 测试。强制 first 旧探针达到 4851 项未溢出，其重复选择日志显示一直选择已访问的同一项，不能据此判定 VM 死循环。
- `message_fade.inc` 根据 `4858c0`、`485790`、`4856a0`、`485560`、`46bbe0`、`46b5d0`、`48c200` 实现独立私有消息精灵；原版 15ms tick、EffectSpeed 0/1/2 和 flag 0x4000 已测。保留目标 Alpha，RGB 两项分别 /255 取整；绘制底图每帧恢复以避免重复混色。主机回归与 Switch 构建通过，交付仍为开发预览。

- `31/528/9` 的前置逻辑已补：`param_change.h` 按 49d680 计算四项目标、float 步长和第四项对总数的影响，参数按低 16 位无符号读取。`ax_count_boundaries` 按 4dd8e0 处理双循环和边界计数，实际 act15.ax 第0轨为59个分界（覆盖脚本100）。动作9当前仅预检查，不提交计划或消费参数；窗口、队列、音效生命周期仍待接通。
- `ax_tick_native` 增加 4decb0 的事件编号与边界延迟 arg-1 语义，独立于已有播放入口；尚未全局启用。新增 `param-change-test`，覆盖真实轨道计数、双循环、无限循环拒绝、截断输入、无符号参数与事件/延迟，主机回归通过。

## 最新状态（2026-09-21，覆盖同接口旧记录）

- `31/812` 已按 `4f9bb0` 实现 24 字节场景名保存/恢复（byte 3570..3593、sys70、CP932/strcpy_s 边界）；`4f60e0` 图像加载允许合法 layer0，`hage_scmode.mes` 的 `bg04.akb` 同步/异步装载专项通过。
- （历史记录）信件回想退出曾停在原生 `31/320 (4fae70)` CScMode/CHageScMode 场景选择模态；该停点已在最新状态中接通。旧哈希仅用于追溯，不能覆盖最新交付结果。
- 主机回归、Switch 编译、`git diff --check` 通过；最新交付主入口 SHA-256 见文末状态。仍未做 Switch 实机测试，不能称完整路线完成。

## 最新状态（2026-09-21，31/320 已接通）

- `31/320 (4fae70)` 不再停在未知接口：前端模态显示原版 `kisaku_DL_seanemode_p.akb` / `p2`，列出600槽目录并按 SceneData 完成字节判定锁定状态；确认已解锁槽走精确回放槽，取消向 VM 返回 `-1`。
- 普通/另一模式的信件回想退出、场景名 `31/812`、layer0 背景和31/320选择均已接通；回放使用现有 SceneData 重建与进度合并。主机专项、完整回归、Switch编译通过，最新 NRO SHA-256 为 `8e434e3036a27810dea644c4e266af955387cc13630d11837297262895b6d1e1`。
- 全路线、后期脚本及其他未核对原生接口仍不能视为完成；不启动PC程序，不把编译结果当作Switch实机验证。

## 最新状态（覆盖上方同接口的历史待办）

- `31/528/9` 参数演出、普通/扩展 AX 原生 tick 已接通；详见 `reports/porting.md` 2026-09-21 的后续记录。全新存档探针3417次调用、2733帧、212次文本、11次选择返回标题；不是完整路线验证。
- 消息合成器已使用 `kisaku_DL_mes_p.akb` 六按钮和 `(0,396,640,84)` 背景。`481be0` 清空整个640×84文字表面，原先只清560×54导致右侧残图；576×54缓冲区仍保留防止越界。
- `31/30` 绘制、原生上下翻页、内侧热区、禁用项和 SELECT 配色已接入。`31/14` 的参考游戏分支、原生系统菜单对话框和部分消息时序仍待独立核对，不得称全部窗口已还原。
- 声音换算统一为原版0..104和二次dB公式；实际 logo.wav/potapota.wav PCM 与混音非零测试通过。主机回归、ASan/UBSan、Switch构建通过，实机声音与完整路线尚未验证。
- AKB32位始终保留第四通道；0x80000000是裁剪外背景填充，24位常量Alpha取flags低8位。真实dialog素材透明像素及全2191张解码已测。原生退出/返回标题CDialog图集、坐标、直通BGRA合成和鼠标/触摸/方向确认已接入，消息按钮1已从设置动作8纠正为退出动作6。
- 普通消息显示/隐藏恢复15ms、EffectSpeed 6/3/0步与0x4000的12步；逐字ShowSpeed默认86、瞬显124，自动翻页AutoMesSpeed默认52，并按累计文本计算原版时限。新探针仍3417调用/212文本/11选择/栈0返回标题，因时序修正为2441帧；前端专项、ASan/UBSan与Switch构建通过。菜单按钮错峰运动/姓名前缀与其他消息模式尚未补齐。
- 设置已按CConfig原版五张AKB/四张AREA重建三页和33角色子页，包含默认值、禁用、草稿/取消/失败回滚；取消错误byte8190写入。RGB/Alpha上限224，设置预览255/224与消息皮肤255/184不同。Settings API共64项，旧参考专用索引保留但不由CConfig写回。切页/进退动画及滑块试听仍待补齐。
- 语音角色过滤按464140/466630的33项映射，静音仍保留历史语音名。游戏音频保留原始PCM，在输出混音中实时应用设置，channel3独立HVolume/IsHEffect；真实logo、BGM、语音和音效静音恢复测试通过。不能把软件输出作为实机扬声器验证。
- 回看使用kisaku_DL_backlog_p.akb及真实九区AREA，六行底部对齐、单条滚动、原版412像素滑块、方向焦点、整条语音点击、鼠标/触摸已接通；64条扁平文本/单语音历史仍非完整原生命令记录。多语音、文字阴影状态和完整记录恢复尚待补齐。
- 存读档已改原版9192/600/15000，移除参考地图及_m改名，按478240写raw+1002的45字节元数据；前三个真实检查点全新运行时读取、可见RGB逐像素及下一句恢复通过。原版100槽/重叠页签/角色簿/备注与读档确认已接通；跨槽进度合并、参数预览/人物详情、参数/日记私有状态与后期恢复仍未完成。
- 19/4颜色键参数已纠正：弹出顺序第九项是RGB透明色，第十项是copyAlpha。原先写反导致绿色块及0x000001像素丢失。用户照片对应ev01.akb，49个近黑像素已定位；真实b00an.akb的135144个绿色键及copyAlpha两模式逐像素验证。最新NRO及验证限制见reports/porting.md；仍是开发预览。
- 31/526/1按4fd915纠正为无附加参数；动作2按47b380/47b220/47b020实现地点标签，撤回错误CLetter淡化。构建静态提取523808处40个CP932标签；layer7(160,336,352,32)暂存，两私有336×32精灵，位置(608−8×字节数,16)。仅恢复覆盖矩形，避免回滚别处消息更新；动作0仍不支持。新的全新存档探针3417调用、2427帧、212文本、11选择返回标题（local/key-final-probe.log），不是完整路线通关。旧探针曾经过错误526/2映射，应以此轮记录为准。


### 最新修复（2026-09-21，覆盖上方历史状态）

- 参数条私有602×112表面呈现/恢复按49fc40客户端30/59/88/112行裁剪，隐藏行不再外露；结束音只响一次，先恢复背景再撤销显示状态。
- 31/520必须用原版应用初始化后的source layer9（4b5661→428ae0），不是构造器layer8；动作1..4弹出bank/cell，动作12弹出bank/cell/target，首帧后目标恢复0；动作2启动state0，state1为边界停止。13/12首帧接通，4ddcc0操作数宽度均为4字节。照片中的双ELF图块已在主机实际公司门口检查点消失。
- 全局进度合并/恢复、参数与日记私有状态及31/528/20/21已接通；目录损坏和新底图截断均拒绝且不替换现态。便携存档版本5含同代.scene底图及0xfffb资源上下文；这是移植扩展，旧档缺失的信息不保证补回。首三个检查点及参数805/read11858检查点从全新标题读档可见RGB差异0，白框消失。
- 主机完整回归、bootstrap/save_runtime ASan/UBSan、Switch编译已通过，交付主入口和兼容名同步。探针3417调用/2427帧/212文本/11选择/栈0返回标题。完整路线、后期存档及Switch实机未验证；仍是开发预览。详细地址、验证日志和最新哈希见reports/porting.md。

- 2026-09-21：31/520/5、8、9、10、11 已接通独立扩展 AX 等待、边界暂停与恢复；普通/扩展等待均按 flag0x20 和取消输入0x101决定跳过。4057c0/4058b0 是轨道登记列表，不是事件队列；13 与31/520的启动/停止/暂停/恢复维护登记次数，31/1011/11按5042d0核对条件成员与精确登记总数。13/1..12先校验再消费参数。主机专项、既有部分剧情探针、Switch构建记录见 reports/porting.md 最新节；完整路线与其他未核对包装调用仍不能视作完成。

- 2026-09-21 后续：31/43/3 的 type2/3 已实现，覆盖旧记录中的未支持边界。type2保留旧文本并淡入新文本；type3按46b1e0/46a520/46a650使用03.bin与512/steps阈值步长。两者收尾只覆盖当前消息矩形；type1仍整面替换。实际遮罩、三档速度、像素Alpha和错误参数保留专项通过，详见 reports/porting.md 最新节。

- 2026-09-21 消息细节：482b20姓名前缀即时复制已接通（CP932 816d/816e，正文从姓名后逐字显示）；483d10/483b80菜单四按钮分别按6/8/10/12与14/12/10/8步展开/收起，原版15ms、速度档位、flag0x4000与跳过规则已测。运动期间隔离菜单命中与对话推进。覆盖旧记录中的这两项待办，见 reports/porting.md 最新节。

- 2026-09-21 信件页：31/525/0、3 按48aed0/48acd0接通进入与清页，2按48adf0退出；三私有表面保留原始背景，RGB饱和减90，24像素文字，独立15ms及32/64/256步长。三档速度、重复清页及退出像素专项通过。动作1正文模态仍不支持并保留参数；已核对的正文相关函数及后续工作见 reports/porting.md 最新节。

- 2026-09-21 信件正文：31/525/1已接通48afa0模态，01.bin按489760/489aa0逐行揭示，三档速度、连续段落游标、一次确认全显并返回0、隐藏/恢复、回看进退和自动/已读跳过已测。4f5210负数编号视为已读，byte4010==1覆盖为27680，等待清/返回置flag0x80。覆盖上一条动作1未支持记录；信件存档重建与回想专用退出对话仍未完成，暂不允许信件状态保存。见reports/porting.md最新节。

- 2026-09-21 信件存档：0xfffe扩展第三项mode2标记CLetter，加载从原生MES检查点重建同页前序段落到目标已读编号，保留mode1旧小说档。只开放可见、完整显示、非负编号的信件正文保存。真实memo.mes四个段落从新标题读档、下一段及退出到普通对话RGB差异0；普通存档与bootstrap回归通过。覆盖上一条信件存档待办，但多层/后期路线及Switch实机未验证，详见reports/porting.md。

- 2026-09-21 信件回想取消：byte4010非零、隐藏信件后再次取消弹原版CDialog mode7（4b3ba0/4bf530），No/取消恢复、Yes按bank1[61]切换scene.mes或hage_scmode.mes并清调用栈，缺资源保留当前模态。真实图集/输入/两种切换及存档回归通过；下游普通模式scene.mes+0x10c仍停31/812/1（4f9bb0），另一模式hage_scmode.mes+0x3c9仍缺图层，不能称回想菜单完成。具体待办见reports/porting.md最新节。

- 2026-09-22 设置试听：按4c2180/4c23c0/4c25e0接通四类音量滑块及原版样本，普通音效与channel3独立。使用只读PCM/独立游标，设置草稿不写盘，退出恢复SDL剧情队列，松手释放临时试听；手柄使用250ms释放适配。仍沿用设置时暂停剧情的移植策略，不称所有原生音轨时钟完全还原。真实样本/音量/取消/输入专项和前端ASan/UBSan通过，详情及交付哈希见reports/porting.md最新节。切页/进退动画、完整回看及保龄球创建/运行仍未完成。

- 2026-09-22 设置演出：4c8800/4c82f0/4c69e0/4c67c0的进入/退出/双阶段切页、4c6640/4c6480角色语音子页淡化已接通。46bbe0对应12/6/0步及flag0x4000强制24步，15ms时钟；Shift/Ctrl快进，普通确认/取消隔离，触摸归属包含动画状态。往返像素及前端ASan/UBSan专项通过，覆盖上条动画待办；完整回看、保龄球创建/运行和全部路线仍未完成，见reports/porting.md。

- 2026-09-22 存档参数预览：按00474500/004747a0从存档 words 重建四种裁剪高度的参数缩略图，展开时显示602×112原始窗口并按24/12/6/0步淡入；坏数据拒绝且不改当前VM/参数表面。参数展开期间输入隔离，Shift/Ctrl按原版102/103快进，确认/取消、触摸和右键关闭已接通。主机回归、ASan/UBSan、Switch构建、资源审计、交付同步和git diff --check通过；最新NRO哈希见reports/porting.md。角色簿group5详情页、保龄球创建/运行、全部路线及Switch实机仍未完成。

- 2026-09-22 角色簿详情入口：group5按原版0x500..0x507热区和存档解锁字节进入独立模态，已解锁时加载真实statfr.akb及八套角色stat*状态条并隔离确认/取消/触摸输入；未解锁只提示且不修改VM。入口和素材解码专项通过，保龄球创建/运行、全部路线及Switch实机仍未完成。

- 2026-09-22 最新覆盖：31/11已修正原版bank/cell/target和双轨启动ABI；31/522每周面板保留前周、工作文字行、问号32级渐变及20ms移动已接通。参数演出隔离点击/菜单，不能点击跳过。31/612动作0/1已接通交互式保龄球，包含真实素材、九名对手物理球路、两半场、计分、角色/球瓶/扫瓶动画、AX与音效、教程/结果/满分及VM返回；动作2清理安全。原始bowl1st/bowl2nd脚本直接入口测试通过，不等于全路线或Switch实机验证。完整回看、31/523逐帧时序、角色簿动态细节等仍未完成，见reports/remaining-work.md；最终验证与产物哈希见reports/porting.md最新节。

- 2026-09-22 接手后：31/523两模式已分六步呈现，按508540逐步等待及16倍加速，VM/输入/保存隔离已接通；修正layer2最小高度180。60Hz下独立20ms等待量化为2帧（普通共12帧），非PC精确时间验证。主机完整回归、专项ASan/UBSan、Switch与SD打包通过；后续仍按remaining-work补回看、角色簿和未核对接口。

- 2026-09-22 Codex接手：flag0x200通用命令捕获与选项临时VM隔离已接通；23/8已按4fe060确认无VM返回值、无状态修改。完整主机回归、回看专项ASan/UBSan和Switch构建通过，详见reports/porting.md。23/5/7、安全重放、绘制状态、多语音及前端/存档统一仍未完成，不得把命令捕获等同完整回看。

- 2026-09-22 实时回看前端已接原生命令重放：独立VM/独立raw、文字颜色/位置/换行、10/0字号、17/5多语音收集及顺序播放，未知调用显式失败且不改剧情状态。旧存档保留扁平历史兼容；23/1同时清空旧历史避免复活。真实开场记录、两段语音和70条记录滚动已测。原生命令存档统一、阴影/跨记录状态、23/5/7地址语义等仍未完成；详见reports/backlog-native.md与remaining-work.md。

- 2026-09-22 附录入口：31/110 mode0..3 的标题/附录布局已接通，原始open.mes往返专项、主机完整回归、ASan/UBSan、Switch构建及打包通过。附录内容仍有明确阻塞：CG31/310、音乐31/340、视频31/70、秃作切换14/12；回想可进入场景模态。不得把菜单接通称为附录全部实现，详见reports/remaining-work.md。文字阴影按用户要求不再列为待实现。

- 2026-09-22 日期/周任务/参数演出：31/524改为(16,16)独立120×128图层，选择重绘后持续显示；31/522/4按原版先弹item再group，纠正问号残留及结果图片文字不出现。参数结束音按播放时钟结束，不能等待保留供混音的PCM释放；点击仍不跳过。--calendar专项、完整主机回归、ASan/UBSan、Switch构建通过，实机和全部剧情组合未验证，见reports/porting.md。

- 2026-09-22 标题入口修复：31/527按4f9dc0消费动作并请求真实存读档模态；有效便携存档恢复bank1[60]可用状态。31/111原版初始化确认、当前模式检查点事务失效及31/1020每周设置清理已接通。14/12全量FLAG恢复、31/1013菜单状态使秃作标题往返及第一句剧情通过。完整主机、标题/前端ASan/UBSan和Switch构建通过；全秃作路线及实机未验证。附录剩余实际接口为31/310、31/340、31/70（此前漏算MES加法的编号已纠正），详见reports/porting.md。

- 2026-09-22 回看存档：0xfffd v2已保存/恢复全部原生命令、语音标记、空槽、索引与录制位；v1扁平历史兼容，读档先校验暂存、重建后提交。70条记录和两段语音、真实前三个检查点及下一句、坏存档隔离、ASan/UBSan和Switch构建通过。跨记录状态及23/5/7仍待实现；详见reports/backlog-native.md最新节，实机未验证。
- 2026-09-22：31/310 CG鉴赏按鬼作自身4fb090/49b2e0与秃作4aba10/4ab220接通，普通307条、秃作27条分类/变体与隔离大图脚本播放通过；交付NRO SHA f50415f0fc77916a30f4687455718a768814ac455d96ab3f8429e82188a30f34。音乐31/340、视频31/70和Switch实机仍未验证。

- 2026-09-22：31/320选择器撤除旧khistory检查点桥接，按4735ca/4a7bf0实现普通bank1[12..15]+返回0、秃作page*256+slot及取消-1；4012模态标记、完成字节、20组分支子列表、分类/页签方向键和秃作layer8第四页图集接通。主机夹具确认原始MES转入普通回放与sc_hage30.mes；进度为隔离夹具设置，不是全路线自然解锁。条件缩略图重排、悬停轮播和进退动画仍未完成，详见reports/porting.md最新节。

- 2026-09-22 高清呈现回归：present_gles改用自有RGBA纹理并完整恢复SDL顶点/VBO/纹理/裁剪等状态，修复旧BGRA直接采样及污染SDL缓存的路径；960×720文字不以低清回退掩盖失败。历史页跳过被遮住的剧情后处理。新增真实Mesa GLES文字/菜单逐像素回归及GL状态夹具，Switch实机仍未验证。轮播动画开发差异暂存local/pending-scene-animation.patch和local/pending-scene-mode-thumbnails.inc，未混入修复包。

- 2026-09-23：高清呈现CPU准备由present_worker在Switch core 2执行，主线程仅在此期间混音/SDL清屏并在使用结果前等待；不得在任务进行中推进VM、改设置/字体或释放runtime。语音core 1、图像解码core 2不变。合成去重、逐行遮挡比较、GPU .bgr通道转换、静态底图/CG缓存和高清文字变化行上传已接通；保留60 FPS目标和高清画质。真实SDL/GLES逐像素与ThreadSanitizer专项通过，实机性能尚未验证，详见reports/porting.md。

- 同引擎复用入口：[高清呈现与多核心移植笔记](reports/高清呈现与多核心移植笔记.md)。涉及高清文字、呈现线程或GL缓存时参考其中的所有权契约与验证边界；不可照搬《鬼作》坐标，也不可把现有锐化路径称为已验证的官方算法一致实现。

- 2026-09-23：正常GLES底图改为限幅Catmull-Rom双三次重建，highp、9次合并采样+4次中央限幅，保留可选锐化、独立高清文字及多核/上传缓存；CPU后备仍使用原算法。实际Mesa对独立16点参考最大误差1/255，真实SDL六面板和文字上传回归通过；Switch编译通过，实机性能未验证。算法和迁移注意事项已补入高清呈现与多核心移植笔记第13节。

- 2026-09-23：正常GLES底图在受限双三次放大后新增输出尺寸3×3亮度边缘锐化，`Display/EdgeStrength`默认55、0恢复旧CASStrength单pass；非FSR。RGBA8/FBO与纹理方向、FRAMEBUFFER_BINDING恢复、弱纹理及实际SDL菜单/HQ字层回归通过；CPU后备和独立模态图片不包含此新pass。Switch构建/打包通过，实机性能与画质未验证。复用笔记第14节及porting.md记录最新证据/哈希。

- 2026-09-23：可恢复FSR1试用，GLES默认EASU+RCAS（AMD公式FP32 GLES2适配，显式采样/精确倒数替代gather/位近似，非SDK位精确）。PresentFilter=edge恢复旧两阶段；FSRSharpness默认90→0.2 stops，范围0..100。afbc2d9原NRO备份在交付/恢复版本；MIT声明随THIRD_PARTY.md交付。GLES参考、菜单/HQ回归和Switch编译通过，实机未验证，详见笔记第15节/porting.md。

- 2026-09-23：用户反馈FSR90噪点增多，已恢复初版默认FSRSharpness80（0.4 stops）；EASU/RCAS保留，显式INI配置仍优先。笔记第16节覆盖此前默认90记录，GLES80像素/HQ/菜单回归通过，实机改善未验证。
